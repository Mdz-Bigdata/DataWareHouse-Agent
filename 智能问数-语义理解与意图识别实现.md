# 智能问数：语义理解与意图识别 · 实现级设计文档

> 本文是《智能问数架构设计.md》的下钻篇，聚焦**可落地的工程实现**：
> 术语词典建模、Schema 向量库字段设计、置信度阈值策略、语义路由代码原型。
> 目标：拿到本文即可开工。

---

## 目录

1. [模块总览与数据流](#1-模块总览与数据流)
2. [意图识别：语义路由实现](#2-意图识别语义路由实现)
3. [业务术语词典：建模与检索](#3-业务术语词典建模与检索)
4. [Schema 向量库：字段设计与召回](#4-schema-向量库字段设计与召回)
5. [实体抽取与时间归一化](#5-实体抽取与时间归一化)
6. [置信度体系与澄清策略](#6-置信度体系与澄清策略)
7. [上下文改写实现](#7-上下文改写实现)
8. [完整 Pipeline 编排](#8-完整-pipeline-编排)
9. [存储与索引选型](#9-存储与索引选型)
10. [冷启动与持续迭代](#10-冷启动与持续迭代)

---

## 1. 模块总览与数据流

```
                    ┌──────────────┐
用户问题 + 会话历史 → │ 上下文改写   │ → 独立完整问题
                    └──────────────┘
                           ↓
                    ┌──────────────┐
                    │ 意图识别     │ → intent + sub_intent + confidence
                    │ (语义路由)   │
                    └──────────────┘
                           ↓
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
  ┌──────────┐      ┌──────────┐       ┌──────────┐
  │实体抽取  │      │时间归一化│       │术语词典  │
  │(LLM+NER) │      │(确定性)  │       │(向量检索)│
  └──────────┘      └──────────┘       └──────────┘
        └──────────────────┼──────────────────┘
                           ↓
                    ┌──────────────┐
                    │Schema Linking│ → 选定 metrics/dims/filters
                    │(RAG召回+精排)│    + 每项置信度
                    └──────────────┘
                           ↓
                    ┌──────────────┐
                    │置信度决策    │ → 执行 / 澄清 / 拒答
                    └──────────────┘
```

**统一数据契约（贯穿全流程的中间对象）：**

```json
{
  "raw_query": "那前三名品类呢",
  "rewritten_query": "华东区销售额同比增长最快的前三个品类",
  "intent": {"type": "data_query", "sub": "ranking+yoy", "confidence": 0.94},
  "entities": {
    "metrics": [{"term": "销售额", "mapped": "metric.gmv", "conf": 0.91}],
    "dimensions": [{"term": "品类", "mapped": "dim.category", "conf": 0.97}],
    "filters": [{"term": "华东区", "mapped": "dim.region='East'", "conf": 0.95}],
    "time": {"type": "last_month", "resolved": ["2026-06-01","2026-06-30"], "conf": 1.0},
    "comparison": {"type": "yoy", "conf": 0.9}
  },
  "limit": 3,
  "order": "desc",
  "overall_confidence": 0.90,
  "decision": "execute"
}
```

---

## 2. 意图识别：语义路由实现

### 2.1 意图定义（配置化，非硬编码）

```yaml
# intents.yaml
intents:
  - id: data_query
    desc: 查询业务数据
    examples:
      - "看下上个月的销售额"
      - "华东区有多少订单"
      - "各品类的利润排名"
    sub_intents:
      - id: detail       # 明细
        examples: ["列出所有退款订单", "看下张三的订单明细"]
        sql_pattern: "SELECT ... (无聚合)"
      - id: aggregation  # 聚合
        examples: ["总销售额是多少", "平均客单价"]
        sql_pattern: "GROUP BY + AGG"
      - id: ranking      # 排名
        examples: ["销售额最高的前十个门店", "哪个品类卖得最好"]
        sql_pattern: "ORDER BY + LIMIT"
      - id: trend        # 趋势
        examples: ["最近30天的日活趋势", "每月销售走势"]
        sql_pattern: "时间维度 GROUP BY"
      - id: comparison   # 对比
        examples: ["同比增长多少", "环比上月的变化"]
        sql_pattern: "窗口函数 LAG / 自连接"
      - id: proportion   # 占比
        examples: ["各渠道占比", "品类销售构成"]
        sql_pattern: "SUM() OVER()"
      - id: attribution  # 归因
        examples: ["为什么这个月GMV下降了", "销售额下滑的原因"]
        sql_pattern: "多维下钻 (Agentic 多步)"

  - id: metadata_query
    desc: 询问指标/字段的定义
    examples: ["有哪些指标可以查", "复购率是怎么算的", "这个表有什么字段"]

  - id: operation
    desc: 执行操作
    examples: ["导出为Excel", "设置每日推送", "把这个存成报表"]

  - id: chitchat
    desc: 闲聊或无关
    examples: ["你好", "你能干嘛", "今天天气"]
```

### 2.2 语义路由核心代码（Python 原型）

```python
import numpy as np
from typing import List, Dict

class SemanticRouter:
    """向量召回意图候选 + LLM 精判"""

    def __init__(self, embed_model, llm, intent_config: dict):
        self.embed = embed_model
        self.llm = llm
        self.intents = intent_config["intents"]
        self._build_index()

    def _build_index(self):
        """把所有意图样例向量化，建成 (向量, 意图id, 子意图id) 索引"""
        self.vectors, self.labels = [], []
        for intent in self.intents:
            for ex in intent.get("examples", []):
                self.vectors.append(self.embed(ex))
                self.labels.append((intent["id"], None))
            for sub in intent.get("sub_intents", []):
                for ex in sub.get("examples", []):
                    self.vectors.append(self.embed(ex))
                    self.labels.append((intent["id"], sub["id"]))
        self.vectors = np.array(self.vectors)

    def recall(self, query: str, top_k: int = 5) -> List[Dict]:
        """第一步：向量召回 Top-K 候选意图"""
        qv = self.embed(query)
        sims = self.vectors @ qv / (
            np.linalg.norm(self.vectors, axis=1) * np.linalg.norm(qv) + 1e-8
        )
        idx = np.argsort(-sims)[:top_k]
        return [{"intent": self.labels[i], "score": float(sims[i])} for i in idx]

    def route(self, query: str) -> Dict:
        """第二步：LLM 在候选里精判（避免相似样例误召回）"""
        candidates = self.recall(query)

        # 快速通道：Top1 分数极高且与 Top2 差距大 → 直接采信，省一次 LLM 调用
        if candidates[0]["score"] > 0.92 and \
           candidates[0]["score"] - candidates[1]["score"] > 0.15:
            intent, sub = candidates[0]["intent"]
            return {"type": intent, "sub": sub, "confidence": candidates[0]["score"]}

        # LLM 精判
        prompt = self._build_prompt(query, candidates)
        result = self.llm.json(prompt)  # 约束输出 JSON
        return result

    def _build_prompt(self, query: str, candidates: List[Dict]) -> str:
        cand_str = "\n".join(
            f"- {c['intent'][0]}/{c['intent'][1]} (相似度{c['score']:.2f})"
            for c in candidates
        )
        return f"""判断用户问题属于以下哪个意图，输出JSON。
用户问题：{query}
候选意图：
{cand_str}

输出格式：{{"type": "意图id", "sub": "子意图id或null", "confidence": 0.0-1.0}}
若都不匹配，type 设为 "chitchat"。"""
```

### 2.3 为什么用"召回+精判"而非纯 LLM 或纯向量

| 方案 | 问题 |
|---|---|
| 纯关键词 | "为什么下降"和"下降了多少"都含"下降"，分不清归因vs对比 |
| 纯向量 Top1 | 相似样例易误召回（"看下明细"vs"看下趋势"向量很近） |
| 纯 LLM 全量分类 | 意图多时 Prompt 长、慢、贵 |
| **召回+精判** | 向量收敛候选(快) + LLM 精判(准) + 快速通道(省)，三者兼得 |

---

## 3. 业务术语词典：建模与检索

### 3.1 数据模型

```yaml
# glossary.yaml — 每条术语的完整字段
- id: term_001
  canonical: 销冠                    # 标准术语
  aliases: [销售冠军, 头号销售, 卖得最好的]  # 同义词/变体
  type: composite                    # simple(单概念) / composite(组合)
  maps_to:                           # 映射到语义层概念
    metric: metric.gmv
    modifier: {rank: 1, order: desc}
  scope: [sales]                     # 适用业务域（防跨域误用）
  examples: ["谁是这个月的销冠", "华东区销冠是哪个门店"]
  priority: 10                       # 冲突时的优先级
  status: active

- id: term_002
  canonical: 大盘
  aliases: [整体, 全局, 总体, 大盘数据]
  type: simple
  maps_to: {scope: global, filters: []}   # 无维度过滤
  examples: ["大盘GMV怎么样"]

- id: term_003
  canonical: 华东区
  aliases: [华东, 江浙沪, 长三角]
  type: dimension_value
  maps_to: {dimension: dim.region, value: "East"}
  examples: ["华东区销售额"]

- id: term_004
  canonical: 复购率
  aliases: [重复购买率, 回购率]
  type: metric_ref
  maps_to: {metric: metric.repurchase_rate}   # 复用中台已定义口径，绝不重新发明
  definition: "统计周期内重复购买用户数 / 总购买用户数"
  examples: ["复购率是多少"]
```

### 3.2 检索流程（同义词召回 + LLM 判定）

```python
class GlossaryResolver:
    def __init__(self, embed_model, llm, glossary: list):
        self.embed = embed_model
        self.llm = llm
        self.terms = glossary
        # 把 canonical + 所有 aliases 都建向量索引
        self.index = []
        for t in glossary:
            for surface in [t["canonical"]] + t.get("aliases", []):
                self.index.append({"vector": self.embed(surface),
                                    "surface": surface, "term": t})

    def resolve(self, phrase: str, biz_scope: str = None, threshold=0.75):
        """返回 phrase 命中的术语 + 置信度"""
        pv = self.embed(phrase)
        scored = []
        for item in self.index:
            sim = self._cosine(pv, item["vector"])
            # 业务域过滤：跨域术语降权
            if biz_scope and biz_scope not in item["term"].get("scope", [biz_scope]):
                sim *= 0.5
            scored.append((sim, item))
        scored.sort(key=lambda x: -x[0])

        best_sim, best = scored[0]
        if best_sim < threshold:
            return None  # 未命中，交给 Schema Linking 兜底
        # 多个高分候选 → LLM 判定
        high = [s for s in scored if s[0] > threshold]
        if len(high) > 1 and high[0][0] - high[1][0] < 0.08:
            return self._llm_disambiguate(phrase, high)
        return {"term": best["term"], "confidence": best_sim}
```

### 3.3 冷启动挖掘（怎么把词典从 0 建起来）

```
来源1  历史 Query 日志   → 高频名词短语聚类 → 人工确认标准术语
来源2  BI 报表/看板标题  → 报表名往往就是业务术语（"华东复购看板"）
来源3  数据字典/指标平台 → 已有指标名和别名直接导入
来源4  用户澄清/纠正记录  → 每次澄清都沉淀新的别名映射
```

---

## 4. Schema 向量库：字段设计与召回

### 4.1 向量库文档结构（每个可检索单元）

Schema Linking 的准确率 = 召回质量。召回质量 = **每个单元的语义描述质量**。

```json
{
  "unit_id": "metric.gmv",
  "unit_type": "metric",            // table / column / metric / dimension / enum_value
  "name": "gmv",
  "display_name": "销售额(GMV)",
  "aliases": ["成交额", "销售金额", "营业额"],
  "description": "订单成交总金额，含税不含运费，仅计已支付订单",
  "definition_sql": "SUM(amount) WHERE status='paid'",   // 口径可见
  "data_type": "decimal",
  "sample_values": [12580.50, 3200.00],
  "related_dimensions": ["dim.region", "dim.category", "dim.date"],
  "biz_domain": "sales",
  "usage_examples": ["查销售额", "各区域GMV对比"],
  "embedding_text": "销售额 GMV 成交额 营业额 订单成交总金额含税不含运费仅已支付订单",  // 用于向量化的拼接文本
  "vector": [...]
}
```

**embedding_text 拼接策略（决定召回效果）：**
```
embedding_text = display_name + aliases + description 关键词
```
> 不要只用字段英文名 `gmv` 向量化——用户说的是"销售额"，两者向量距离远。必须把中文别名和描述拼进去。

### 4.2 列值索引（解决"上海"定位到 city 字段）

高基数枚举维度（城市、品类、渠道）单独建值索引：

```json
{
  "unit_id": "enum.dim_city.shanghai",
  "unit_type": "enum_value",
  "belongs_to": "dim.city",
  "value": "上海",
  "aliases": ["沪", "魔都"],
  "embedding_text": "上海 沪 魔都 城市"
}
```
用户说"上海的订单"→ 召回到 `enum.dim_city.shanghai` → 反查出该用 `dim.city='上海'`。

### 4.3 分层召回 + 精排代码

```python
class SchemaLinker:
    def __init__(self, vector_store, llm):
        self.vs = vector_store
        self.llm = llm

    def link(self, query: str, entities: dict, biz_domain: str = None) -> dict:
        # 1) 多路召回：对每个抽取实体分别检索，合并去重
        candidates = {"metrics": [], "dimensions": [], "filters": []}
        for m in entities.get("metrics", []):
            hits = self.vs.search(m["term"], filter={"unit_type": "metric",
                                  "biz_domain": biz_domain}, top_k=8)
            candidates["metrics"].extend(hits)
        for d in entities.get("dimensions", []):
            hits = self.vs.search(d["term"], filter={"unit_type":
                                  ["dimension", "column"]}, top_k=8)
            candidates["dimensions"].extend(hits)
        for f in entities.get("filters", []):
            # 过滤条件优先查枚举值索引
            hits = self.vs.search(f["term"], filter={"unit_type":
                                  ["enum_value", "dimension"]}, top_k=8)
            candidates["filters"].extend(hits)

        # 2) LLM 精排：在召回子集里选定最终映射，并给每项打置信度
        prompt = f"""基于用户问题和候选Schema，选定要用的指标/维度/过滤条件。
用户问题：{query}
抽取实体：{entities}
候选指标：{self._fmt(candidates['metrics'])}
候选维度：{self._fmt(candidates['dimensions'])}
候选过滤：{self._fmt(candidates['filters'])}

规则：
- 只能从候选里选，禁止使用未出现的字段（防幻觉）
- 每个映射给出 confidence(0-1)
- 无法匹配的实体标记 unmatched

输出JSON：{{"metrics":[{{"term":..,"unit_id":..,"conf":..}}],
           "dimensions":[...], "filters":[...], "unmatched":[...]}}"""
        return self.llm.json(prompt)
```

### 4.4 M-Schema 表示（喂给生成层的紧凑格式）

比裸 DDL 省 token 且信息更全：

```
【表】fact_orders (订单事实表)
  region     VARCHAR  区域, 样例:['East','South'], 外键→dim_region
  category   VARCHAR  品类, 样例:['数码','服饰']
  amount     DECIMAL  实付金额(元,含税不含运费)
  status     VARCHAR  订单状态, 样例:['paid','refunded']
  order_date DATE     下单日期
【指标】gmv = SUM(amount) WHERE status='paid'
【关系】fact_orders.region = dim_region.code
```

---

## 5. 实体抽取与时间归一化

### 5.1 实体抽取（LLM few-shot + 词典增强）

```python
EXTRACT_PROMPT = """从问题中抽取数据查询要素，输出JSON。

示例：
Q: 上个月华东区各品类销售额排名
A: {"metrics":["销售额"], "dimensions":["品类"],
    "filters":[{"term":"华东区"}], "time":{"type":"last_month"},
    "order":"desc", "analysis":"ranking"}

Q: 近7天日活趋势
A: {"metrics":["日活"], "dimensions":["日期"],
    "time":{"type":"last_n_days","n":7}, "analysis":"trend"}

现在处理：
Q: {query}
A:"""
```

### 5.2 时间归一化 —— 确定性代码（严禁 LLM 计算日期）

```python
from datetime import date
from dateutil.relativedelta import relativedelta
import calendar

def resolve_time(time_type: str, n: int = None, ref: date = None) -> dict:
    """LLM 只识别 type，真实区间由此函数确定性计算"""
    ref = ref or date.today()
    if time_type == "today":
        return {"start": ref, "end": ref}
    if time_type == "yesterday":
        y = ref - relativedelta(days=1)
        return {"start": y, "end": y}
    if time_type == "last_n_days":
        return {"start": ref - relativedelta(days=n), "end": ref - relativedelta(days=1)}
    if time_type == "last_month":
        first_this = ref.replace(day=1)
        last_month_end = first_this - relativedelta(days=1)
        return {"start": last_month_end.replace(day=1), "end": last_month_end}
    if time_type == "this_quarter":
        q = (ref.month - 1) // 3
        start = date(ref.year, q * 3 + 1, 1)
        return {"start": start, "end": ref}
    if time_type == "this_year":
        return {"start": date(ref.year, 1, 1), "end": ref}
    raise ValueError(f"未知时间类型: {time_type}")

def yoy_period(cur: dict) -> dict:
    """同比：去年同期"""
    return {"start": cur["start"] - relativedelta(years=1),
            "end": cur["end"] - relativedelta(years=1)}

def mom_period(cur: dict) -> dict:
    """环比：上一周期"""
    return {"start": cur["start"] - relativedelta(months=1),
            "end": cur["end"] - relativedelta(months=1)}
```

> **口径坑提醒**：财年、自然周（周一起还是周日起）、"上月同期到今天"这类需求要单独配置，别硬套自然月。

---

## 6. 置信度体系与澄清策略

### 6.1 综合置信度计算

```python
def overall_confidence(parsed: dict) -> float:
    """加权：Schema Linking 权重最高（错这里=静默错误）"""
    weights = {"intent": 0.15, "metrics": 0.35, "dimensions": 0.20,
               "filters": 0.20, "time": 0.10}
    scores = {
        "intent": parsed["intent"]["confidence"],
        "metrics": min([m["conf"] for m in parsed["entities"]["metrics"]] or [1]),
        "dimensions": min([d["conf"] for d in parsed["entities"]["dimensions"]] or [1]),
        "filters": min([f["conf"] for f in parsed["entities"]["filters"]] or [1]),
        "time": parsed["entities"]["time"]["conf"],
    }
    return sum(weights[k] * scores[k] for k in weights)
```

### 6.2 阈值分级决策

```python
def decide(parsed: dict) -> dict:
    conf = overall_confidence(parsed)
    unmatched = parsed.get("unmatched", [])

    if unmatched:                          # 有实体没映射上
        return {"action": "clarify_missing", "targets": unmatched}
    if conf >= 0.85:                       # 高置信 → 直接执行
        return {"action": "execute"}
    if conf >= 0.60:                       # 中置信 → 给选项澄清
        return {"action": "clarify_options",
                "candidates": _low_conf_items(parsed)}
    return {"action": "reject_guide"}      # 低置信 → 引导

def _low_conf_items(parsed, floor=0.75):
    """挑出置信度低的项，为它们生成澄清选项"""
    items = []
    for cat in ["metrics", "dimensions", "filters"]:
        for e in parsed["entities"][cat]:
            if e["conf"] < floor:
                items.append(e)
    return items
```

### 6.3 澄清话术（给选项，不开放式提问）

```
❌ 差："您说的销售额是什么意思？"           （用户不知道怎么答）
✅ 好："销售额有两种口径，您指的是：
        ① GMV(成交额，含未付款)
        ② 实付金额(仅已支付)
        请回复 ①或②"                       （降低负担，可点选）
```

阈值建议初始值：`执行≥0.85 / 澄清≥0.60 / 拒答<0.60`。上线后用**黄金评测集**回归调参——阈值太高澄清率高伤体验，太低静默错误率高伤信任。

---

## 7. 上下文改写实现

```python
REWRITE_PROMPT = """将用户的最新问题改写为不依赖上下文的独立完整问题。
补全指代（它/那个/这些）和省略的主体，保留原意，不要新增用户未提的条件。

对话历史：
{history}

最新问题：{current}

独立问题："""

def rewrite(current: str, history: list, llm) -> str:
    # 无历史或问题已完整则跳过（省调用）
    if not history or _is_self_contained(current):
        return current
    recent = history[-3:]  # 只取近3轮，控制 token
    return llm.text(REWRITE_PROMPT.format(
        history=_fmt_history(recent), current=current))

def _is_self_contained(q: str) -> bool:
    """启发式：不含指代词且长度足够，视为完整"""
    pronouns = ["它", "那个", "这个", "他们", "那些", "这些", "呢"]
    return not any(p in q for p in pronouns) and len(q) > 8
```

---

## 8. 完整 Pipeline 编排

```python
class NL2QueryPipeline:
    def __init__(self, router, glossary, extractor, linker, llm):
        self.router = router
        self.glossary = glossary
        self.extractor = extractor
        self.linker = linker
        self.llm = llm

    def parse(self, raw_query: str, history: list, biz_domain: str) -> dict:
        # Step1 上下文改写
        query = rewrite(raw_query, history, self.llm)

        # Step2 意图识别
        intent = self.router.route(query)
        if intent["type"] == "chitchat":
            return {"decision": {"action": "reject_guide"}, "intent": intent}
        if intent["type"] == "metadata_query":
            return {"decision": {"action": "answer_metadata"}, "intent": intent}

        # Step3 实体抽取（并行：抽取 + 时间可同步做）
        entities = self.extractor.extract(query)

        # Step4 时间归一化（确定性）
        if entities.get("time"):
            t = entities["time"]
            entities["time"]["resolved"] = resolve_time(t["type"], t.get("n"))
            entities["time"]["conf"] = 1.0

        # Step5 术语词典优先解析（命中的直接映射，省 Schema 检索）
        for cat in ["metrics", "dimensions", "filters"]:
            for e in entities.get(cat, []):
                hit = self.glossary.resolve(e["term"], biz_domain)
                if hit:
                    e["mapped"] = hit["term"]["maps_to"]
                    e["conf"] = hit["confidence"]
                    e["source"] = "glossary"

        # Step6 Schema Linking（词典未命中的走 RAG）
        linked = self.linker.link(query, entities, biz_domain)
        self._merge(entities, linked)

        # Step7 置信度决策
        parsed = {"rewritten_query": query, "intent": intent,
                  "entities": entities, "unmatched": linked.get("unmatched", [])}
        parsed["overall_confidence"] = overall_confidence(parsed)
        parsed["decision"] = decide(parsed)
        return parsed
```

**并行优化点**：意图识别、术语检索、Schema 召回三者对同一 query 独立，可并发执行降延迟。

---

## 9. 存储与索引选型

| 组件 | 选型建议 | 说明 |
|---|---|---|
| 向量库 | Milvus / Qdrant / pgvector | Schema、术语、意图样例、few-shot 分 collection |
| Embedding 模型 | bge-large-zh / m3e / OpenAI text-embedding-3 | 中文场景优先中文模型，注意与召回文本语言一致 |
| 元数据仓库 | PostgreSQL | 术语词典、语义层定义、权限、审计日志 |
| 缓存 | Redis | L1结果 / L2 SQL / L3召回 三级缓存 |
| 会话状态 | Redis | 多轮上下文、澄清中间态 |

**向量库 collection 划分：**
```
collection_schema     → 表/字段/指标/维度  (Schema Linking)
collection_enum       → 枚举值            (列值索引)
collection_glossary   → 业务术语          (词典检索)
collection_intent     → 意图样例          (语义路由)
collection_fewshot    → 问题-SQL样例对    (生成层检索增强)
```

---

## 10. 冷启动与持续迭代

### 10.1 冷启动清单

```
□ 语义层定义 20-50 个核心指标（先覆盖高频，别追求全）
□ 从 BI 报表/数据字典导入初版术语词典
□ 每张核心表补齐中文注释和字段描述（决定召回质量）
□ 高基数维度建枚举值索引
□ 每个意图准备 5-10 条样例
□ 标注 50-100 条黄金评测集（问题-标准SQL-标准结果）
```

### 10.2 反馈闭环（数据飞轮）

```
用户点踩/纠正
    ↓
归因分析（是意图错？映射错？时间错？）
    ↓
分流沉淀：
  ├── 意图错   → 补充意图样例到 collection_intent
  ├── 术语错   → 新增别名到 glossary
  ├── 映射错   → 补充 few-shot 到 collection_fewshot
  └── 口径错   → 修正语义层指标定义
    ↓
黄金评测集回归测试（确认没引入退化）
    ↓
灰度发布
```

### 10.3 监控指标（上线后必看）

```
意图分类准确率        目标 > 92%
Schema Linking 准确率  目标 > 85%
一次答对率            目标 > 75%
澄清率                目标 15-25%（太高伤体验，太低可能在猜）
静默错误率            目标 < 3%（最重要，需人工抽检核对）
P95 延迟             目标 < 3s
```

---

## 附：语义理解 × 意图识别 关系图

```
                意图识别                     语义理解
                   │                           │
            "怎么处理这句话"              "这句话说的什么概念"
                   │                           │
        ┌──────────┴──────────┐     ┌──────────┴──────────┐
        │ 分类：查询/元数据/  │     │ 术语词典（黑话映射） │
        │       操作/闲聊     │     │ 实体抽取（NER）      │
        │ 子类：明细/排名/    │     │ 时间归一化（确定性） │
        │       趋势/对比/归因│     │ Schema Linking(RAG)  │
        └──────────┬──────────┘     └──────────┬──────────┘
                   │                           │
                   └────────► 置信度决策 ◄──────┘
                        执行 / 澄清 / 拒答
```

**一句话总结：**
> 意图识别用**语义路由**（向量召回+LLM精判）快速定链路；
> 语义理解靠**术语词典 + RAG Schema Linking + 确定性时间归一化**跨越语义鸿沟；
> 两者的输出都汇入**置信度决策**——不确定就澄清，绝不猜错。

---

*文档版本 v1.0 · 配套《智能问数架构设计.md》下钻篇*
