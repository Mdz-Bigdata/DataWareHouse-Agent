# 智能问数：语义层建模 · 实现级设计文档

> 配套系列第 06 篇。语义层是整个智能问数的**知识底座**，是最重要的架构决策。
> 它统一口径、收敛搜索空间、内置权限。没有语义层，一切校验都是补丁。

---

## 目录

1. [语义层的本质与价值](#1-语义层的本质与价值)
2. [核心模型：指标、维度、口径](#2-核心模型指标维度口径)
3. [指标建模详解](#3-指标建模详解)
4. [维度建模详解](#4-维度建模详解)
5. [口径统一：消灭静默错误的根](#5-口径统一消灭静默错误的根)
6. [权限内置](#6-权限内置)
7. [语义层 → 多方言 SQL 编译](#7-语义层--多方言-sql-编译)
8. [选型：自建 vs 开源](#8-选型自建-vs-开源)
9. [建模规范与治理](#9-建模规范与治理)

---

## 1. 语义层的本质与价值

### 1.1 一句话定义

语义层（Semantic Layer / Metrics Store / Headless BI）是一个**位于物理数据表和上层应用之间的抽象层**，它用业务语言（指标、维度）定义"数据是什么"，屏蔽物理实现"数据存在哪、怎么算"。

```
业务应用（问数/BI/报表）
        ↓  只认识 指标 + 维度
   ┌─────────────────┐
   │    语义层        │  ← gmv = SUM(amount) WHERE status='paid'
   │  指标/维度/口径   │     region = dim_region.name
   └─────────────────┘
        ↓  编译成物理SQL
物理数据表（ODS/DWD/DWS）
```

### 1.2 为什么它是智能问数最重要的决策

| 无语义层（LLM直接查物理表） | 有语义层 |
|---|---|
| "销售额"每次可能算出不同口径 | 口径唯一，全局一致 |
| LLM 在上千表里选，准确率低 | 在几十个指标里选，准确率高 |
| 权限散落各处，易越权 | 权限统一内置 |
| 物理表变更，上层全崩 | 改映射即可，上层无感 |
| 静默错误靠事后校验补 | 从源头杜绝口径错误 |

> **核心洞察**：智能问数的静默错误，**根源是口径不统一**。语义层从架构上消灭它，比任何下游校验都根本。

---

## 2. 核心模型：指标、维度、口径

语义层三大基本元素：

```
指标 Metric      "算什么" —— 可聚合的数值   gmv / uv / 复购率
维度 Dimension   "按什么分/筛" —— 分组或过滤 region / category / date
口径 Definition  "怎么算" —— 指标的确切计算逻辑  SUM(amount) WHERE status='paid'
```

三者组合成一次查询：**"按【维度】看【指标】，用【口径】计算"**。

---

## 3. 指标建模详解

### 3.1 指标定义模型

```yaml
# metrics/gmv.yaml
metric:
  id: metric.gmv
  name: gmv
  display_name: 销售额(GMV)
  aliases: [成交额, 营业额, 销售金额]
  description: 订单成交总金额，含税不含运费，仅计已支付订单
  type: simple                        # simple / ratio / derived / cumulative

  # 口径核心：聚合表达式 + 默认过滤
  source_table: dwd_orders            # 建议基于明细宽表，而非直接ODS
  aggregation: SUM                    # SUM / COUNT / COUNT_DISTINCT / AVG / MAX
  expression: amount                  # 被聚合的字段
  default_filters:                    # 口径过滤——用户绝对无法绕过
    - "status = 'paid'"
  time_field: order_date              # 该指标的时间维度字段（供时间过滤用）

  # 可用维度（限定这个指标能按哪些维度分析）
  available_dimensions: [region, category, channel, date, shop]

  # 单位与格式
  format: {unit: "元", decimals: 2, thousands_separator: true}

  # 治理
  owner: 交易域-张三
  biz_domain: sales
  status: certified                   # certified(认证) / draft / deprecated
  visibility: public
```

### 3.2 四种指标类型

```yaml
# 1) simple —— 直接聚合
gmv: {aggregation: SUM, expression: amount, default_filters: ["status='paid'"]}

# 2) ratio —— 比率型（分子/分母，各带口径）
repurchase_rate:
  type: ratio
  numerator:   {aggregation: COUNT_DISTINCT, expression: user_id,
                filters: ["order_count >= 2"]}
  denominator: {aggregation: COUNT_DISTINCT, expression: user_id}
  format: {unit: "%", decimals: 1}

# 3) derived —— 派生（基于其他指标运算）
avg_order_value:
  type: derived
  expression: "metric.gmv / metric.order_count"   # 引用其他指标，口径自动继承

# 4) cumulative —— 累计/时间窗（如MTD、近30日留存）
mtd_gmv:
  type: cumulative
  base_metric: metric.gmv
  window: month_to_date
```

### 3.3 预置复杂分析（防 LLM 现算窗口函数）

同比、环比、留存、漏斗、复购这类，**在语义层预定义为可引用的计算模式**：

```yaml
# 时间对比是指标的能力，不是每次现写
metric.gmv:
  time_comparisons:
    - {type: yoy, label: 同比, offset: {years: -1}}
    - {type: mom, label: 环比, offset: {months: -1}}
    - {type: wow, label: 周环比, offset: {weeks: -1}}
# 上游DSL只需 comparison:{type:yoy}，语义层自动生成CTE双窗对比SQL
```

> 这是防错的关键：**能在语义层预置的复杂逻辑，绝不让 LLM 现场生成。**

---

## 4. 维度建模详解

### 4.1 维度定义模型

```yaml
# dimensions/region.yaml
dimension:
  id: dim.region
  name: region
  display_name: 区域
  aliases: [大区, 地区, 区]
  type: categorical                   # categorical / temporal / geo / numeric_bucket

  # 物理映射
  source: {table: dim_region, key_column: region_code, name_column: region_name}
  # 或直接是事实表字段
  # source: {table: dwd_orders, column: region}

  # 枚举值（用于列值索引 + 澄清选项）
  values:
    - {code: East,  display: 华东, aliases: [华东区, 江浙沪, 长三角]}
    - {code: South, display: 华南, aliases: [华南区, 广深]}
    - {code: North, display: 华北, aliases: [华北区]}

  # 层级（支持下钻）
  hierarchy: [country, region, province, city]

  biz_domain: sales
  visibility: public
```

### 4.2 时间维度（特殊且高频）

```yaml
dimension:
  id: dim.date
  type: temporal
  source: {table: dwd_orders, column: order_date}
  grains: [day, week, month, quarter, year]    # 支持的时间粒度
  # 关联时间维表（含节假日、财年等）
  calendar_table: dim_calendar
  fiscal_year_start: "01-01"                    # 财年起始（非自然年时配置）
  week_start: monday                            # 周起始
```

### 4.3 维度层级与下钻（支撑归因分析）

```yaml
# 归因时需要沿层级下钻
hierarchy_region:
  levels: [country, region, province, city]
  # "华东GMV下降" → 下钻到 province → 定位到"浙江下降最多" → 再下钻 city
```

---

## 5. 口径统一：消灭静默错误的根

### 5.1 同名不同义问题

```
"销售额" 在不同人嘴里：
  运营说的  = GMV（含未付款下单金额）
  财务说的  = 实付金额（已支付）
  老板说的  = 实付 - 退款（净销售额）
```

**语义层的解法：不允许模糊的"销售额"，强制拆成明确指标：**

```yaml
metric.gmv:          {display_name: 成交额GMV,   default_filters: []}
metric.paid_amount:  {display_name: 实付金额,     default_filters: ["status='paid'"]}
metric.net_sales:    {display_name: 净销售额,     type: derived,
                      expression: "metric.paid_amount - metric.refund_amount"}
```

用户说"销售额" → Schema Linking 召回三个候选 → 置信度低 → **触发澄清给选项**（承接系列 05）。

### 5.2 认证指标机制（Certified Metrics）

```
指标状态流转：
  draft(草稿) → review(评审) → certified(认证) → deprecated(废弃)

只有 certified 指标才对问数开放。
  ├── 认证需口径负责人签字
  ├── certified 指标在问数结果里带"✓认证"标识，提升信任
  └── draft 指标仅建模者可见，防止未定稿口径流出
```

### 5.3 口径变更管理

```yaml
# 口径变更必须留痕 + 通知下游
metric.gmv:
  version: 3
  changelog:
    - {version: 3, date: 2026-05-01, change: "含税口径改为含税不含运费",
       author: 张三, impact: "历史对比数据将有差异"}
  # 变更影响的下游：报表、few-shot样例、缓存 → 需级联失效
```

---

## 6. 权限内置

### 6.1 三级权限

```yaml
# 语义层统一管控，问数/BI共用同一套权限
permissions:
  # 1) 指标级：谁能看这个指标
  metric_access:
    metric.profit: {roles: [finance, management]}   # 利润仅财务和管理层

  # 2) 维度/列级：谁能看这个字段
  column_access:
    dim.user_phone: {roles: [admin], mask: partial}  # 手机号脱敏

  # 3) 行级 RLS：只能看自己范围的数据
  row_level_security:
    dwd_orders:
      - {role: region_manager, rule: "region = {user.managed_region}"}
      # 华东经理只能查 region='East' 的数据
```

### 6.2 权限注入（编译期自动加 WHERE）

```python
def row_level_security(self, user, table):
    """返回该用户在该表上的行级过滤条件，编译SQL时自动AND进WHERE"""
    rules = self.rls_config.get(table, [])
    conds = []
    for r in rules:
        if r["role"] in user.roles:
            conds.append(r["rule"].format(user=user))   # 变量替换
    return " AND ".join(conds) if conds else None
# 关键：LLM完全不感知权限，权限在编译期强制注入，无法绕过
```

> **安全红线**：权限在语义层编译期注入，不依赖 LLM。LLM 生成的查询意图里没有、也不需要有权限概念——用户看不到的数据，编译出的 SQL 根本查不到。

---

## 7. 语义层 → 多方言 SQL 编译

### 7.1 一次建模，多引擎输出

```
         语义层定义（引擎无关）
   metric.gmv = SUM(amount) WHERE status='paid'
                    ↓ 编译
   ┌────────────┬────────────┬────────────┐
   │  MySQL     │ ClickHouse │  Presto    │
   │  方言SQL   │  方言SQL   │  方言SQL   │
   └────────────┴────────────┴────────────┘
```

### 7.2 编译接口

```python
class SemanticLayer:
    def compile_metric(self, metric_id, dimensions, filters, time_range, dialect):
        m = self.get_metric(metric_id)
        # 处理派生/比率指标的递归展开
        if m.type == "ratio":
            return self._compile_ratio(m, dimensions, filters, time_range, dialect)
        if m.type == "derived":
            return self._compile_derived(m, dimensions, filters, time_range, dialect)
        # simple
        agg = f"{m.aggregation}({m.expression})"
        where = self._merge_filters(m.default_filters, filters, time_range,
                                    self.rls(current_user, m.source_table))
        group = ", ".join(d.column for d in dimensions)
        return self.dialect_render(agg, m.source_table, where, group, dialect)
```

---

## 8. 选型：自建 vs 开源

| 方案 | 代表 | 适用 |
|---|---|---|
| dbt Semantic Layer | dbt + MetricFlow | 已用 dbt，指标定义随模型走 |
| Cube | Cube.js/Cube Cloud | 需要 API 化指标、多前端复用 |
| 自建 | —— | 已有指标中台/数据中台，深度定制 |
| BI 内置 | Looker(LookML) | 已重度使用 Looker |

**建议：**
- 已有数据中台/指标平台 → **复用它作为语义层**，别重造轮子。关键是把指标定义结构化、可被问数调用。
- 从零开始 → **dbt Semantic Layer 或 Cube** 起步，社区成熟。
- 核心不在选哪个工具，而在**是否真的把口径统一定义、并让问数只走语义层**。

---

## 9. 建模规范与治理

### 9.1 建模规范

```
□ 指标必须有：唯一口径(default_filters) + 中文别名 + 描述 + owner
□ 每个业务词只对应一个明确指标，禁止模糊指标
□ 复杂分析(同比/留存/漏斗)预置为指标能力，不留给下游现算
□ 指标基于明细宽表(DWD)建，而非直接ODS，保证口径一致性
□ 维度枚举值配齐 aliases，供列值索引和澄清选项
□ 权限在语义层定义，问数/BI/报表共用
```

### 9.2 治理流程

```
建模 → 评审(口径负责人签字) → 认证certified → 开放问数
                                    ↓
                          变更走版本管理 + 下游级联失效
                                    ↓
                          定期审计：无人使用的指标下线，
                                    冲突口径合并
```

### 9.3 与问数各模块的接口

```
语义层 提供给：
  ├── Schema Linking  → 指标/维度清单 + embedding_text 素材
  ├── 术语词典        → 指标别名同步
  ├── 生成层编译器    → compile_metric() 出物理SQL
  ├── 权限校验        → allowed_metrics/columns + RLS
  ├── 澄清模块        → 同名指标候选 + 枚举值选项
  └── 可解释交付      → 指标 definition 展示给用户复核
```

---

## 附：语义层核心地位图

```
         用户自然语言问题
                ↓
   理解层(意图/实体/术语) ──┐
                ↓          │ 都依赖语义层提供
   Schema Linking ─────────┤ 指标/维度/别名/权限
                ↓          │
   ┌──────────────────────┴──┐
   │      语义层 (知识底座)     │
   │  指标·维度·口径·权限·层级  │
   └──────────┬───────────────┘
              ↓ compile
        多方言物理SQL
              ↓
          沙箱执行
```

**一句话总结：**
> 语义层是智能问数的**地基**——它用统一口径从源头消灭静默错误，用有限指标收敛 LLM 的搜索空间，用内置权限守住安全底线。
> **建好语义层，问数成功一半；跳过语义层，再强的 LLM 也只是在流沙上盖楼。**

---

*文档版本 v1.0 · 智能问数系列 06 · 语义层建模*
