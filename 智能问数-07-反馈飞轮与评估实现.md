# 智能问数：反馈飞轮与评估体系 · 实现级设计文档

> 配套系列第 07 篇。没有评估就没有优化，没有飞轮就没有进化。
> 本文下钻：黄金评测集构建、错误归因分流、自动化回归、数据飞轮工程实现。

---

## 目录

1. [为什么评估和飞轮是生死线](#1-为什么评估和飞轮是生死线)
2. [黄金评测集构建](#2-黄金评测集构建)
3. [核心评估指标体系](#3-核心评估指标体系)
4. [执行准确率评估实现](#4-执行准确率评估实现)
5. [错误归因分流](#5-错误归因分流)
6. [数据飞轮工程实现](#6-数据飞轮工程实现)
7. [自动化回归与灰度](#7-自动化回归与灰度)
8. [线上监控与告警](#8-线上监控与告警)

---

## 1. 为什么评估和飞轮是生死线

### 1.1 两个残酷现实

```
现实1  智能问数是"长尾问题"，永远有没覆盖的问法
       → 靠一次性开发达不到高准确率，必须持续迭代
现实2  改进A场景常常退化B场景（改Prompt/加样例的副作用）
       → 没有回归评估，就是按下葫芦浮起瓢
```

### 1.2 飞轮闭环

```
   用户使用 → 产生反馈(点赞/点踩/纠正)
       ↑                    ↓
   准确率提升          错误归因分流
       ↑                    ↓
   灰度发布 ← 回归测试 ← 定向优化(补样例/改口径/加别名)
```

> **核心**：智能问数不是"开发完上线"的项目，是"上线才开始"的运营。飞轮转得越快，护城河越深。

---

## 2. 黄金评测集构建

黄金评测集（Golden Set）是评估的基石——**没有它，一切"变好了"都是错觉。**

### 2.1 评测样本结构

```json
{
  "case_id": "gold_001",
  "question": "上个月华东区各品类销售额同比增长前三",
  "context": [],                        // 多轮场景的上文
  "expected": {
    "intent": {"type": "data_query", "sub": "ranking+yoy"},
    "dsl": {"metrics":["gmv"], "dimensions":["category"],
            "filters":[{"field":"region","value":"East"}],
            "comparison":"yoy", "limit":3},
    "sql": "WITH cur AS (...) ...",     // 标准SQL（允许多个等价写法）
    "result_signature": "hash_of_result_set"   // 标准结果指纹
  },
  "difficulty": "hard",                 // easy/medium/hard
  "analysis_type": "comparison",
  "tags": ["同比","排名","多维过滤"],
  "biz_domain": "sales",
  "source": "human_curated"
}
```

### 2.2 覆盖度矩阵（防止评测集有偏）

评测集必须**按难度 × 意图 × 业务域**均衡覆盖：

```
              明细  聚合  排名  趋势  对比  占比  归因
easy(简单)     ✓    ✓    ✓    ✓
medium(中等)        ✓    ✓    ✓    ✓    ✓
hard(复杂)                     ✓    ✓    ✓    ✓
多轮场景        ✓    ✓         ✓         ✓

每格至少 10 条，总量建议 300-500 条起步，随长尾问题扩充。
```

### 2.3 构建来源

```
真实Query日志采样    最重要——反映真实分布，别只测你想得到的问法
人工构造边界case     故意造歧义、口径混淆、复杂嵌套，测防错能力
线上失败case回收     用户点踩的问题 → 修正后转为评测样本
业务专家提供         各业务域专家出"应该能答对"的代表性问题
```

> **关键**：评测集要包含**"应该拒答/澄清"的负样本**，不只是"应该答对"的正样本。能正确澄清歧义，和能答对一样重要。

---

## 3. 核心评估指标体系

### 3.1 分层指标

```
【端到端指标】北极星
  一次答对率(1st-turn Accuracy)   目标>75%   问一次就对，不用澄清/重问
  用户采纳率                       目标>80%   结果被用户接受/使用
  静默错误率                       目标<3%    ★最危险★ 需人工抽检

【链路分解指标】定位问题在哪一环
  上下文改写准确率                 目标>90%
  意图分类准确率                   目标>92%
  实体抽取F1                       目标>88%
  时间归一化准确率                 目标>99%   确定性代码应接近满分
  Schema Linking准确率             目标>85%   命门
  SQL执行准确率(EX)                目标>80%
  幻觉字段率                       目标<1%

【体验指标】
  澄清率                           15-25%    太高伤体验，太低可能在猜
  澄清有效率                       澄清后用户能答对的比例
  拒答合理率                       该拒的拒了(负样本)
  P95延迟                          <3s
```

### 3.2 为什么用执行准确率(EX)而非精确匹配(EM)

```
精确匹配EM   SQL文本完全一致才算对 → 太严苛，等价写法算错，无意义
执行准确率EX 执行结果与标准结果一致就算对 → 贴近真实业务价值 ★推荐★

  例：ORDER BY a,b 和 ORDER BY b,a 文本不同，但若结果集相同 → EX判对
```

---

## 4. 执行准确率评估实现

### 4.1 结果比对（核心难点：等价判定）

```python
def eval_execution_accuracy(generated_sql, expected, executor):
    """执行两条SQL，比对结果集是否等价"""
    try:
        gen_result = executor.execute(generated_sql, timeout=30)
    except Exception as e:
        return {"correct": False, "reason": f"执行失败: {e}", "type": "exec_error"}

    exp_result = executor.execute(expected["sql"])
    if result_equivalent(gen_result, exp_result):
        return {"correct": True}
    return {"correct": False, "reason": "结果不一致", "type": "result_mismatch",
            "gen": gen_result[:5], "exp": exp_result[:5]}

def result_equivalent(r1, r2, order_sensitive=None):
    """结果集等价判定，处理列序/行序/浮点误差"""
    if len(r1) != len(r2):
        return False
    # 列集合一致（忽略列序）
    if set(_col_names(r1)) != set(_col_names(r2)):
        return False
    # 归一化：排序类保持行序，非排序类按行内容排序后比
    n1 = _normalize_rows(r1, order_sensitive)
    n2 = _normalize_rows(r2, order_sensitive)
    for row1, row2 in zip(n1, n2):
        for k in row1:
            if isinstance(row1[k], float):
                if abs(row1[k] - row2[k]) > 1e-6:   # 浮点容差
                    return False
            elif row1[k] != row2[k]:
                return False
    return True
```

### 4.2 批量评估 Runner

```python
def run_eval(golden_set, pipeline, executor) -> dict:
    results = {"total": len(golden_set), "correct": 0, "by_type": {},
               "by_difficulty": {}, "failures": []}
    for case in golden_set:
        parsed = pipeline.parse(case["question"], case.get("context", []),
                                case["biz_domain"])
        # 分层判定
        intent_ok = parsed["intent"]["type"] == case["expected"]["intent"]["type"]
        gen = pipeline.generate(parsed)
        ex = eval_execution_accuracy(gen["sql"], case["expected"], executor)

        _record(results, case, intent_ok, ex)
        if not ex["correct"]:
            results["failures"].append({
                "case_id": case["case_id"], "question": case["question"],
                "expected_dsl": case["expected"]["dsl"],
                "got": {"intent": parsed["intent"], "sql": gen.get("sql")},
                "error": ex, "diagnosis": diagnose(case, parsed, gen, ex)
            })
    results["accuracy"] = results["correct"] / results["total"]
    return results
```

---

## 5. 错误归因分流

失败了要能**自动定位错在哪一环**，才能定向优化。这是飞轮的引擎。

### 5.1 归因决策树

```python
def diagnose(case, parsed, gen, ex) -> dict:
    """逐环比对，定位第一个出错的环节 + 给出修复方向"""
    exp = case["expected"]

    # 环1：上下文改写
    if case.get("context") and not _rewrite_ok(parsed["rewritten_query"], case):
        return {"stage": "上下文改写", "fix": "补充多轮改写few-shot"}

    # 环2：意图
    if parsed["intent"]["type"] != exp["intent"]["type"]:
        return {"stage": "意图识别", "fix": "补充意图样例到语义路由库",
                "detail": f"期望{exp['intent']['type']}，得到{parsed['intent']['type']}"}

    # 环3：时间归一化
    if not _time_ok(parsed, exp):
        return {"stage": "时间归一化", "fix": "检查时间解析规则(财年/周边界)",
                "severity": "high"}   # 确定性代码出错=bug，优先级最高

    # 环4：Schema Linking（最常见的错）
    linking_err = _diagnose_linking(parsed, exp)
    if linking_err:
        return linking_err   # 见系列05的归因：召回缺失/口径混淆/幻觉/选错

    # 环5：SQL生成
    if ex["type"] == "exec_error":
        return {"stage": "SQL生成", "fix": "检查编译器模板/补生成few-shot",
                "sql_error": ex["reason"]}
    if ex["type"] == "result_mismatch":
        return {"stage": "SQL逻辑", "fix": "结果不一致，检查聚合/过滤/JOIN",
                "severity": "high"}   # 可能是静默错误！

    return {"stage": "unknown", "fix": "人工复核"}
```

### 5.2 归因统计 → 优化优先级

```python
def prioritize_fixes(eval_result) -> list:
    """按错误类型聚合，算出改哪里ROI最高"""
    stage_counts = {}
    for f in eval_result["failures"]:
        stage = f["diagnosis"]["stage"]
        stage_counts[stage] = stage_counts.get(stage, 0) + 1
    # 按错误数排序，高severity的加权
    ranked = sorted(stage_counts.items(), key=lambda x: -x[1])
    return [{"stage": s, "error_count": c,
             "fix": _fix_for_stage(s),
             "roi": c / eval_result["total"]} for s, c in ranked]
# 输出示例：
# [{"stage":"Schema Linking","error_count":23,"fix":"补字段别名","roi":0.15},
#  {"stage":"意图识别","error_count":8,"fix":"补意图样例","roi":0.05}]
```

---

## 6. 数据飞轮工程实现

### 6.1 反馈采集

```python
class FeedbackCollector:
    def collect(self, session_id, query, sql, result, feedback):
        """feedback: {type: like/dislike/correct, corrected_sql?, comment?}"""
        record = {
            "session_id": session_id, "query": query, "sql": sql,
            "feedback_type": feedback["type"], "timestamp": now(),
            "parsed_trace": self._get_trace(session_id),  # 全链路中间结果
        }
        if feedback["type"] == "dislike":
            record["diagnosis"] = None   # 待离线归因
        if feedback["type"] == "correct":
            record["corrected_sql"] = feedback["corrected_sql"]  # 金子！
        self.store.save(record)
```

### 6.2 反馈分流处理（飞轮核心）

```python
def process_feedback_batch(feedback_records, pipeline):
    """定期批处理：归因 → 分流 → 沉淀到对应知识库"""
    actions = {"intent_examples": [], "glossary_aliases": [],
               "few_shots": [], "metric_fixes": [], "schema_enrich": []}

    for r in feedback_records:
        if r["feedback_type"] == "like":
            # 点赞的正样本 → 转为few-shot（提升相似问题准确率）
            actions["few_shots"].append(_to_few_shot(r))
            continue

        diag = diagnose_from_trace(r["parsed_trace"], r.get("corrected_sql"))
        stage = diag["stage"]

        if stage == "意图识别":
            actions["intent_examples"].append({"text": r["query"],
                                               "intent": diag["correct_intent"]})
        elif stage == "Schema Linking" and diag["type"] == "召回缺失":
            # 用户其实想要的字段 → 补别名
            actions["glossary_aliases"].append({"term": diag["user_term"],
                                                "maps_to": diag["correct_unit"]})
        elif stage == "Schema Linking" and diag["type"] == "口径混淆":
            actions["metric_fixes"].append({"issue": "口径歧义",
                                           "metrics": diag["confused_metrics"]})
        elif stage in ("SQL生成", "SQL逻辑") and r.get("corrected_sql"):
            # 纠正后的SQL → 高质量few-shot
            actions["few_shots"].append(_to_few_shot(r, verified=True))

    return actions

def apply_actions(actions):
    """人工审核后应用到各知识库"""
    for ex in actions["intent_examples"]:
        intent_store.add(ex)              # → 语义路由库
    for al in actions["glossary_aliases"]:
        glossary.add_alias(al)            # → 术语词典
    for fs in actions["few_shots"]:
        few_shot_store.add(fs)            # → few-shot库
    for mf in actions["metric_fixes"]:
        create_ticket(mf, assignee="指标负责人")  # → 语义层人工处理
```

### 6.3 飞轮的四条沉淀路径

```
点踩/纠正 → 归因 → 分流：
  ├── 意图错   → 语义路由库加样例      (自动，审核后生效)
  ├── 召回缺失 → 术语词典加别名        (自动，审核后生效)
  ├── 口径错   → 语义层指标修正        (人工，建工单)
  └── 生成错   → few-shot库加样例      (自动，纠正SQL人工验证)

点赞 → 直接转 few-shot（正反馈也是养料）
```

> **闭环红线**：自动沉淀的样本**必须人工审核后才生效**，否则错误反馈会污染知识库，飞轮变成"劣币驱逐良币"。

---

## 7. 自动化回归与灰度

### 7.1 回归门禁（防止改进引入退化）

```python
def regression_gate(new_config, golden_set, baseline_result):
    """任何知识库/Prompt变更，必须过回归才能上线"""
    new_result = run_eval(golden_set, build_pipeline(new_config), executor)

    checks = {
        "overall": new_result["accuracy"] >= baseline_result["accuracy"] - 0.005,
        # 分难度不退化
        "no_regression": all(
            new_result["by_difficulty"][d] >= baseline_result["by_difficulty"][d] - 0.02
            for d in ["easy", "medium", "hard"]
        ),
        # 静默错误率不上升（最关键）
        "silent_error": new_result.get("silent_error_rate", 0) <=
                        baseline_result.get("silent_error_rate", 0),
    }
    passed = all(checks.values())
    return {"pass": passed, "checks": checks,
            "delta": new_result["accuracy"] - baseline_result["accuracy"],
            # 找出新退化的case
            "new_failures": _diff_failures(baseline_result, new_result)}
```

### 7.2 灰度发布

```
知识库变更 → 回归门禁通过 → 灰度5%流量 → 监控关键指标
                                              ↓
                              指标不降 → 逐步放量50%→100%
                              指标下降 → 自动回滚 + 告警
```

---

## 8. 线上监控与告警

### 8.1 实时监控面板

```
┌─────────────────────────────────────────────┐
│  今日问数量  一次答对率  澄清率  P95延迟      │
│    1,240      78.3%      19%     2.1s        │
├─────────────────────────────────────────────┤
│  各环节准确率趋势（7日）                       │
│  意图 ▁▂▃▅▆▇▇  Schema ▃▄▅▅▆▆▇  执行 ▂▃▄▅▅▆▆ │
├─────────────────────────────────────────────┤
│  Top失败问题（待归因）                         │
│  "XX指标环比"×12  "YY下钻"×8  ...            │
└─────────────────────────────────────────────┘
```

### 8.2 告警规则

```python
ALERTS = [
    {"metric": "silent_error_rate", "op": ">", "threshold": 0.05,
     "level": "critical", "action": "立即人工介入+考虑回滚"},   # 静默错误飙升最危险
    {"metric": "1st_turn_accuracy", "op": "<", "threshold": 0.70,
     "level": "warning", "action": "排查近期变更"},
    {"metric": "p95_latency", "op": ">", "threshold": 5.0,
     "level": "warning", "action": "检查慢查询/模型延迟"},
    {"metric": "clarify_rate", "op": ">", "threshold": 0.35,
     "level": "info", "action": "澄清过多，检查Schema质量"},
    {"metric": "exec_error_rate", "op": ">", "threshold": 0.08,
     "level": "warning", "action": "检查生成层/Schema变更"},
]
```

### 8.3 静默错误的专项抽检

静默错误无法自动检测（结果看起来合理），必须**人工抽检**：

```
每日随机抽取 N 条"成功"的问答 → 业务专家核对口径和结果 →
发现静默错误 → 立即归因 + 转为评测负样本 + 排查同类问题
```

> 静默错误率是所有指标里**唯一必须靠人工保障**的，因为它的本质是"系统以为对了但其实错了"，系统自己发现不了。

---

## 附：飞轮全景图

```
        用户问数
           ↓
     ┌──────────┐   点赞 ────────────→ few-shot库
     │ 反馈采集 │   点踩/纠正 ─┐
     └──────────┘              ↓
                          ┌──────────┐
                          │ 错误归因 │
                          └────┬─────┘
              ┌────────────────┼────────────────┐
              ↓        ↓        ↓        ↓
          意图样例  术语别名  语义层口径  few-shot
              └────────────────┼────────────────┘
                          人工审核
                               ↓
                      黄金评测集回归门禁
                               ↓
                        灰度发布(可回滚)
                               ↓
                        准确率提升 → 更多使用 → 更多反馈（循环）
```

**一句话总结：**
> 智能问数的竞争力不在初始准确率，而在**飞轮转速**——用黄金评测集守住不退化，用错误归因指明优化方向，用反馈闭环持续喂养知识库。
> 而这一切的底线是：**静默错误率必须靠人工抽检死守**，因为它是系统唯一看不见的敌人。

---

*文档版本 v1.0 · 智能问数系列 07 · 反馈飞轮与评估*
