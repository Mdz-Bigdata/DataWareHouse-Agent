# 大模型实现智能问数详细架构设计与详细流程

## 1. 文档目标

本文给出一套面向企业级数据分析场景的“大模型智能问数”完整技术方案，重点覆盖：

- 总体架构设计
- 端到端详细流程
- 核心语义理解手段与技术
- 核心意图识别手段与技术
- 元数据与语义层体系设计
- 查询规划、生成、校验、执行与解释机制
- 多轮对话、权限控制、反馈学习与工程落地建议

本文假设目标场景是企业内部 BI / 数据中台 / 数据服务平台的自然语言问数能力建设，而不是开放域聊天机器人。

---

## 2. 建设目标

智能问数系统的核心目标不是“像人在回答”，而是“把自然语言问题稳定地转成正确的数据分析请求并返回可信结果”。

### 2.1 业务目标

- 降低业务用户使用 SQL 和 BI 工具的门槛
- 缩短从“提出问题”到“获得结论”的链路
- 统一指标口径，减少人工解读偏差
- 提升常见数据分析问题的自助化比例
- 将高频标准分析沉淀为平台能力

### 2.2 技术目标

- 支持自然语言转结构化分析请求
- 支持复杂问句、多轮追问、上下文继承
- 支持指标、维度、时间、组织等业务语义理解
- 支持受约束查询生成，避免幻觉字段和错误口径
- 支持权限控制、结果校验、解释和可追责

---

## 3. 设计原则

### 3.1 语义层优先

大模型不能直接替代企业语义层。企业问数中真正决定正确性的，是：

- 指标定义
- 维度定义
- 表关系
- 时间口径
- 组织映射
- 权限边界

### 3.2 结构化中间表示优先

不要让模型直接从自然语言生成 SQL 作为唯一主路径。推荐先生成结构化中间表示（IR / DSL），再转换为 SQL 或指标查询请求。

### 3.3 检索增强优先

模型必须基于企业元数据、语义层、指标目录、表字段说明和历史问句进行 grounding，不能靠参数记忆猜业务。

### 3.4 受约束生成优先

SQL、字段、表、Join 路径、聚合方式都要被系统约束，避免模型自由发挥。

### 3.5 校验与澄清优先

当问题存在歧义、口径冲突或候选结果不明确时，系统应主动澄清，而不是强行猜测。

---

## 4. 总体架构

### 4.1 分层架构总览

```text
用户入口 / 应用入口
  -> 会话接入层
  -> 问题预处理层
  -> 意图识别层
  -> 语义解析层
  -> 语义检索与 Schema Grounding 层
  -> 查询规划层
  -> SQL / DSL 生成层
  -> 权限控制与执行层
  -> 结果校验层
  -> 回答生成与解释层
  -> 多轮状态管理层
  -> 反馈学习与运营分析层
```

### 4.2 核心模块职责

| 模块 | 主要职责 |
|---|---|
| 会话接入层 | 接收问题、身份识别、会话管理、权限上下文注入 |
| 预处理层 | 文本清洗、时间归一、术语标准化、问句改写 |
| 意图识别层 | 判断问题属于查值、趋势、对比、排行、归因等哪类分析 |
| 语义解析层 | 抽取指标、维度、时间、过滤条件、排序、TopN 等槽位 |
| Schema Grounding 层 | 将自然语言概念绑定到真实指标、字段、表和语义对象 |
| 查询规划层 | 形成结构化分析意图和中间表示 |
| 生成层 | 基于 IR 生成 DSL / SQL / API 调用计划 |
| 执行层 | 权限校验、SQL 执行、缓存、限流、审计 |
| 校验层 | 校验语法、口径、结果完整性与异常情况 |
| 解释层 | 用业务语言输出结论、口径和限制说明 |
| 状态层 | 多轮对话上下文继承和修正 |
| 反馈层 | 收集用户反馈和错误样本，持续优化模型与规则 |

---

## 5. 核心数据对象设计

### 5.1 用户请求对象

```json
{
  "request_id": "req_001",
  "tenant_id": "t1",
  "user_id": "u123",
  "role": "regional_manager",
  "session_id": "s001",
  "question": "看华东区最近30天销售额同比下降最快的5个品类",
  "context": {}
}
```

### 5.2 标准化请求对象

```json
{
  "normalized_question": "查询华东区最近30天销售额同比下降最快的5个品类",
  "tokens": ["华东区", "最近30天", "销售额", "同比", "下降最快", "5个", "品类"],
  "time_expressions": ["最近30天"],
  "candidate_terms": ["销售额", "品类", "华东区"]
}
```

### 5.3 中间表示 IR

```json
{
  "intent": ["comparison_analysis", "ranking_analysis"],
  "metric": {
    "name": "sales_amount",
    "aggregation": "sum"
  },
  "dimensions": ["category"],
  "filters": [
    {"field": "region", "op": "=", "value": "华东区"}
  ],
  "time_range": {
    "type": "relative",
    "value": "last_30_days"
  },
  "comparison": {
    "type": "yoy"
  },
  "sort": {
    "field": "yoy_change",
    "direction": "asc"
  },
  "limit": 5,
  "need_clarification": false
}
```

---

## 6. 端到端详细流程

## 6.1 流程概览

```text
用户提问
  -> 文本预处理
  -> 意图识别
  -> 槽位抽取与语义解析
  -> 元数据检索与 Schema Grounding
  -> 结构化 IR 生成
  -> 查询规划
  -> SQL / DSL 生成
  -> 权限检查与执行
  -> 结果校验
  -> 自然语言解释
  -> 多轮状态保存
  -> 反馈收集
```

## 6.2 流程详解

### Step 1：用户提问

输入：

```text
看华东区最近30天销售额同比下降最快的5个品类
```

系统拿到：

- 当前用户身份
- 所属租户
- 数据权限
- 历史会话状态

### Step 2：文本预处理

处理内容：

- 去噪和纠错
- 数字和量词归一化
- 时间短语标准化
- 口语表达标准化
- 简单同义词替换

例如：

- “最近30天” -> 标准时间表达 `last_30_days`
- “看” -> 分析动作前缀，忽略
- “下降最快” -> 排序语义 `order by delta asc`

### Step 3：意图识别

识别结果：

- `comparison_analysis`
- `ranking_analysis`

解释：

- “同比” 明确说明是对比分析
- “下降最快的5个” 明确说明是排行分析

### Step 4：槽位抽取与语义解析

抽出：

- 指标：销售额
- 组织范围：华东区
- 时间范围：最近30天
- 维度：品类
- 排序：同比降幅由大到小
- 限制：前 5 个

### Step 5：Schema Grounding

基于指标目录、维度目录、表结构说明、字段别名、历史问句样本进行检索，找到：

- 目标指标候选：`sales_amount`
- 目标维度候选：`category`
- 目标组织字段：`region`
- 候选事实表：`dws_sales_order_day`

### Step 6：结构化 IR 生成

系统输出结构化意图，供后续规划与执行。

### Step 7：查询规划

规划器决定：

- 以天或更高粒度取数
- 需要构造去年同期窗口
- 需要按品类聚合
- 需要计算同比变化率
- 需要排序并限制 Top 5

### Step 8：SQL / DSL 生成

根据标准模板或受约束生成器构造查询。

### Step 9：权限校验与执行

执行前检查：

- 用户是否有权查看华东区数据
- 是否允许查看品类级别明细
- SQL 是否超出扫描成本阈值

### Step 10：结果校验

校验项：

- 是否有去年同期基期数据
- 结果是否为空
- 计算字段是否异常
- 排序方向是否符合“下降最快”的语义

### Step 11：结果解释

输出业务化结论，例如：

```text
最近30天华东区销售额同比下降最快的5个品类分别是 A、B、C、D、E。
统计口径为已支付销售额，时间范围为最近30天，对比基期为去年同期。
```

### Step 12：状态持久化与追问支持

保存当前分析状态，支持后续问题：

- “那上海呢”
- “再按品牌拆开”
- “换成利润看一下”

---

## 7. 核心语义理解：手段与技术

语义理解的目标，是把用户自然语言转换成业务系统可执行的语义对象。

## 7.1 词汇层语义理解

### 7.1.1 业务术语词典

建立领域词典，覆盖：

- 指标别名
- 维度别名
- 组织名称
- 常见缩写
- 业务黑话

示例：

| 用户表达 | 标准语义 |
|---|---|
| 销售 | sales_amount / sales_qty / order_count（需消歧） |
| GMV | gross_merchandise_value |
| 新客 | new_customer |
| 华东 | region = 华东区 |

### 7.1.2 同义词归一化

技术手段：

- 规则词典
- embedding 相似匹配
- 召回后 rerank
- 人工维护同义词库

### 7.1.3 时间语义解析

支持：

- 最近 7 天 / 最近 30 天
- 本周 / 上周 / 本月 / 上月
- 去年同期 / 环比上周
- 截止昨天 / 截止当前时点

技术手段：

- 正则表达式
- 时间解析器
- 语义规则引擎
- LLM 补充解析复杂口语时间

## 7.2 句子层语义理解

### 7.2.1 句法分析

适用场景：

- 理解修饰关系
- 明确排序对象
- 区分多个条件作用范围

例如：

“看华东区高客单价品类最近30天销售额排名”

需要识别：

- “高客单价”修饰的是品类
- “最近30天”修饰的是时间窗口
- “排名”修饰的是销售额结果

### 7.2.2 语义角色标注

识别问题中的分析对象、限制条件、目标操作和对比关系。

## 7.3 业务层语义理解

这是企业问数区别于通用问答的关键。

### 7.3.1 指标口径理解

例如“销售额”可能有多种定义：

- 下单金额
- 支付金额
- 发货金额
- 含税金额
- 未税金额

系统不能靠模型猜，必须通过语义层提供标准定义。

### 7.3.2 组织语义理解

例如：

- 北京 -> 城市 / 分公司 / 运营区
- 华东 -> 大区
- 门店 A -> 门店实体

要通过组织元数据完成精确映射。

### 7.3.3 多轮上下文理解

例如：

- “看华东区上周销售额”
- “那上海呢”
- “按门店拆开”
- “同比也看一下”

系统需要继承：

- 指标
- 时间
- 范围
- 维度
- 对比方式

---

## 8. 核心意图识别：手段与技术

意图识别的目标，是确定用户希望进行哪种分析动作。

## 8.1 意图分类体系设计

推荐按分析任务定义意图，而不是按聊天场景定义。

### 8.1.1 一级意图

- 查值类
- 分析类
- 解释类
- 预测类
- 异常类

### 8.1.2 二级意图

- `metric_lookup`：查指标值
- `trend_analysis`：趋势分析
- `comparison_analysis`：同比 / 环比 / 对比
- `ranking_analysis`：排名 / TopN / BottomN
- `breakdown_analysis`：构成 / 拆分 / 分组
- `attribution_analysis`：归因
- `anomaly_detection`：异常检测
- `forecast_analysis`：预测
- `definition_query`：指标口径解释
- `mixed_intent`：复合意图

## 8.2 规则法

### 8.2.1 适用范围

- 强模式关键词
- 高确定性问句
- 低成本首层识别

示例规则：

- 出现“同比”“环比” -> `comparison_analysis`
- 出现“排名”“前10”“Top5” -> `ranking_analysis`
- 出现“趋势”“走势”“变化曲线” -> `trend_analysis`
- 出现“什么是”“口径”“定义” -> `definition_query`

### 8.2.2 优缺点

优点：

- 稳定
- 易解释
- 响应快

缺点：

- 泛化差
- 长尾表达覆盖有限

## 8.3 传统分类模型

### 8.3.1 可选模型

- BERT / RoBERTa / ERNIE 分类模型
- 多标签分类器
- 轻量蒸馏模型

### 8.3.2 适用范围

- 高频稳定场景
- 固定意图集
- 需要低时延和高吞吐场景

## 8.4 LLM 意图识别

### 8.4.1 Few-shot 分类

通过指令和示例让模型输出结构化意图：

```json
{
  "intent": ["comparison_analysis", "ranking_analysis"],
  "confidence": 0.94,
  "need_clarification": false
}
```

### 8.4.2 复合意图拆解

适合长句多目标问题，例如：

“看最近一个月各区域销售额趋势，并找出下降最快的区域，再解释原因。”

可拆成：

1. 趋势分析
2. 排行分析
3. 归因分析

## 8.5 层级意图识别

建议采用：

1. 一级粗分类
2. 二级细分类
3. 是否需要拆成多子任务

这样能提升复杂场景下的稳定性。

## 8.6 多轮意图修正

上下文继承下，很多问句本身是不完整的：

- “那去年呢”
- “改成利润”
- “只看线上”

这类问题必须结合 session state 进行意图修正。

---

## 9. 槽位抽取与语义解析设计

## 9.1 关键槽位

| 槽位 | 示例 |
|---|---|
| metric | 销售额、利润、订单数 |
| dimension | 品类、品牌、门店、区域 |
| filters | 华东区、线上渠道、直营门店 |
| time_range | 最近30天、上月、去年同期 |
| sort | 降序、升序、下降最快 |
| limit | 前5个、Top10 |
| comparison | 同比、环比 |
| granularity | 按天、按周、按月 |
| org_scope | 全国、大区、城市、门店 |

## 9.2 技术路线

### 9.2.1 序列标注

可用：

- BERT + CRF
- BiLSTM + CRF
- span-based extraction

适合抽取：

- 地区
- 品牌
- 渠道
- 时间短语
- 数量词

### 9.2.2 结构化抽取

推荐用 LLM 输出固定 JSON，并配合 schema 校验。

### 9.2.3 规则补强

对于时间、TopN、同比环比等强结构槽位，优先使用规则解析。

---

## 10. 元数据体系设计

元数据是整个系统的“业务操作系统”。没有元数据体系，问数系统难以稳定落地。

## 10.1 元数据分类

### 10.1.1 技术元数据

- 表名、字段名、类型
- 分区字段
- 主键、外键
- 表关系
- 更新时间
- 存储引擎

### 10.1.2 业务元数据

- 指标定义
- 维度定义
- 指标口径
- 时间口径
- 组织映射
- 权限归属

### 10.1.3 语义元数据

- 同义词
- 别名
- 缩写
- 领域术语
- 历史问句映射

### 10.1.4 运行元数据

- 查询日志
- 用户追问记录
- 错误类型
- SQL 执行统计
- 用户反馈

## 10.2 指标元数据设计

```json
{
  "metric_id": "m_sales_amount",
  "metric_name": "sales_amount",
  "display_name": "销售额",
  "aliases": ["成交额", "营收"],
  "definition": "已支付订单金额，含税",
  "aggregation": "sum",
  "source_table": "dws_sales_order_day",
  "source_field": "pay_amount",
  "business_owner": "销售分析团队",
  "time_grain": ["day", "week", "month"],
  "allowed_dimensions": ["region", "category", "brand", "store"],
  "security_level": "internal"
}
```

## 10.3 维度元数据设计

```json
{
  "dimension_id": "d_region",
  "dimension_name": "region",
  "display_name": "区域",
  "aliases": ["地区", "大区", "销售区域"],
  "hierarchy": ["nation", "region", "city", "store"],
  "source_table": "dim_org_region",
  "source_field": "region_name"
}
```

## 10.4 表关系元数据设计

```json
{
  "fact_table": "dws_sales_order_day",
  "joinable_dimensions": [
    {
      "dimension_table": "dim_category",
      "join_key": "category_id",
      "join_type": "left"
    },
    {
      "dimension_table": "dim_region",
      "join_key": "region_id",
      "join_type": "left"
    }
  ]
}
```

## 10.5 问句示例元数据

沉淀高质量历史问句与正确语义结构：

```json
{
  "question": "看华东区最近30天销售额 Top5 品类",
  "intent": ["ranking_analysis"],
  "metric": "sales_amount",
  "dimension": "category",
  "filters": [{"field": "region", "value": "华东区"}],
  "time_range": "last_30_days"
}
```

这类数据可用于：

- few-shot 示例
- 检索增强
- 分类器训练
- 质量回放评估

---

## 11. Schema Grounding 设计

Schema Grounding 是指将自然语言中的业务概念，绑定到真实数据对象。

## 11.1 召回对象

- 指标定义
- 字段说明
- 表结构说明
- 维度枚举值
- 组织树节点
- 历史问句
- FAQ / 口径文档

## 11.2 检索技术组合

### 11.2.1 关键词召回

适合：

- 精确表名
- 字段名
- 指标别名
- 组织名称

### 11.2.2 向量召回

适合：

- 同义表达
- 口语问法
- 长尾业务术语

### 11.2.3 混合召回

推荐：

- BM25 + 向量召回并行
- 合并候选
- rerank 取 TopK

### 11.2.4 rerank

可用：

- Cross-Encoder
- LLM rerank
- 规则打分

排序维度：

- 文本相似度
- 业务优先级
- 历史使用频次
- 当前用户角色相关性

---

## 12. 查询规划与 IR 设计

## 12.1 为什么需要 IR

直接生成 SQL 的问题：

- 难校验
- 难解释
- 难做多引擎适配
- 错误不可定位

IR 的价值：

- 可追踪
- 可校验
- 可解释
- 可复用

## 12.2 IR 字段建议

- intent
- metric
- dimensions
- filters
- time_range
- comparison
- sort
- limit
- granularity
- visualization_hint
- clarification_flag

## 12.3 查询规划器职责

- 判断是否单指标 / 多指标
- 判断是否需要同比基期
- 判断是否需要多步查询
- 判断是否需要先聚合再排序
- 判断是否可以使用已有模板

---

## 13. SQL / DSL 生成设计

## 13.1 优先级策略

### 一级：模板化生成

适合：

- 查值
- 趋势
- TopN
- 同比环比
- 占比分析

### 二级：IR 到 DSL

如果企业已有指标平台或语义查询引擎，优先转 DSL。

### 三级：受约束 LLM SQL 生成

只在模板覆盖不到时使用，并强约束：

- 仅可用候选表字段
- 仅可用允许 Join 路径
- 仅可用许可函数
- 仅可输出固定格式

## 13.2 生成结果示例

```json
{
  "query_plan_type": "sql_template",
  "template": "ranking_yoy_metric_by_dimension",
  "bindings": {
    "metric": "sales_amount",
    "dimension": "category",
    "region": "华东区",
    "time_range": "last_30_days",
    "limit": 5
  }
}
```

---

## 14. 权限控制与执行设计

## 14.1 权限控制维度

- 租户级权限
- 角色级权限
- 行级权限
- 列级权限
- 指标级权限
- 组织级权限

## 14.2 执行前检查

- 用户是否可查该指标
- 用户是否可见该组织范围
- 用户是否可查看明细维度
- 查询是否超出资源配额

## 14.3 执行优化

- 缓存高频查询
- 预聚合表优先
- 常见模板走指标层
- 限制大表全扫
- 限制笛卡尔积风险

---

## 15. 结果校验与纠错设计

## 15.1 校验维度

### 15.1.1 语法校验

- SQL parser 校验
- 非法字段和函数校验

### 15.1.2 语义校验

- 指标与维度是否匹配
- 时间窗口是否合理
- 对比基期是否存在
- 排序方向是否符合问题语义

### 15.1.3 结果校验

- 结果是否为空
- 数值是否异常
- TopN 数量是否正确
- 同比计算结果是否可解释

## 15.2 错误处理策略

- 可自动修复：回退到候选字段 / 模板
- 需要澄清：提示用户补充口径
- 高风险错误：阻断执行并反馈原因

---

## 16. 澄清机制设计

澄清机制是企业问数系统的必备能力。

## 16.1 触发条件

- 指标有多个候选且分数接近
- 组织范围不明确
- 时间口径不明确
- “新增”“活跃”“销售额”等概念有多种定义
- 复合问题超出单轮处理能力

## 16.2 示例

用户：

```text
看新增用户
```

系统应澄清：

```text
“新增用户”当前有两个常用口径：注册用户、首购用户。请确认你要查看哪一种。
```

---

## 17. 多轮对话与上下文状态设计

## 17.1 会话状态对象

```json
{
  "current_metric": "sales_amount",
  "current_time_range": "last_30_days",
  "current_filters": [
    {"field": "region", "value": "华东区"}
  ],
  "current_dimension": "category",
  "current_comparison": "yoy"
}
```

## 17.2 支持场景

- 省略提问
- 条件替换
- 维度切换
- 指标切换
- 时间切换
- 对比方式追加

## 17.3 典型示例

- Q1：看华东区最近30天销售额
- Q2：那上海呢
- Q3：按门店拆开
- Q4：同比也看一下

每一轮都应在前文状态上增量修改，而不是重新从零解析。

---

## 18. 反馈学习与持续优化设计

## 18.1 反馈来源

- 用户点赞 / 点踩
- 追问率
- 改问率
- 结果导出率
- 人工修正 SQL
- 澄清接受率
- 空结果投诉

## 18.2 可优化对象

- 同义词词典
- 意图识别器
- 槽位抽取器
- Grounding 检索排序
- 模板覆盖率
- Few-shot 示例库
- 澄清触发规则

## 18.3 学习闭环

```text
线上日志
  -> 错误样本归类
  -> 标注正确 IR / SQL
  -> 更新词典 / 示例 / 模型
  -> 回归评测
  -> 灰度发布
```

---

## 19. 推荐的模型与技术分工

## 19.1 模型分工建议

| 能力 | 推荐方式 |
|---|---|
| 时间解析 | 规则 + 时间解析器 |
| 强模式意图识别 | 规则 |
| 高频意图分类 | 小型分类模型 |
| 复杂意图拆解 | 主 LLM |
| 槽位抽取 | 序列标注 + LLM 结构化抽取 |
| Schema Grounding | 混合检索 + rerank |
| 查询规划 | 规则规划器 + LLM 辅助 |
| SQL 生成 | 模板 / DSL / 受约束 LLM |
| 结果解释 | 主 LLM |

## 19.2 生产建议

- 高频标准分析尽量模板化
- 长尾复杂问句才使用大模型规划
- 能规则解决的不要交给模型
- 能由语义层定义的不要让模型猜

---

## 20. 服务拆分建议

建议拆成如下服务：

- `query-gateway-service`
- `query-preprocess-service`
- `intent-service`
- `semantic-parse-service`
- `metadata-retrieval-service`
- `grounding-service`
- `query-planner-service`
- `query-generator-service`
- `query-validator-service`
- `query-executor-service`
- `answer-generation-service`
- `dialog-state-service`
- `feedback-learning-service`

---

## 21. 评估指标体系

## 21.1 离线评估

- 意图识别准确率
- 多标签意图 F1
- 槽位抽取准确率
- Schema Grounding Top1 / TopK 命中率
- SQL executable rate
- SQL semantic correctness
- 澄清触发准确率

## 21.2 在线评估

- 首轮命中率
- 任务完成率
- 用户追问率
- 用户改问率
- 空结果率
- 人工介入率
- 查询平均时延
- 单次问数成本

## 21.3 业务关键指标

- 高价值指标问数正确率
- 高风险口径问题拦截率
- 业务自助化率
- 数据分析响应时长下降比例

---

## 22. 风险点与应对措施

| 风险 | 表现 | 应对 |
|---|---|---|
| 指标口径歧义 | 查出来但口径不对 | 建强语义层，必要时澄清 |
| 幻觉字段 | 模型编造列名 | 受约束生成 + SQL parser |
| Schema 复杂 | 错误 Join | Join 路径白名单 |
| 多轮污染 | 继承错误上下文 | 状态可视化 + 状态重置机制 |
| 权限越界 | 查到无权数据 | 执行前权限强校验 |
| 空结果误导 | 用户以为无业务 | 结果解释里提示数据缺失或条件过严 |

---

## 23. 推荐落地路径

## 23.1 第一阶段：单主题域 MVP

选择单一主题域，例如销售分析，先做：

- 20~50 个核心指标
- 10~20 个核心维度
- 高频问题模板
- 单轮问数
- 基础语义层

## 23.2 第二阶段：多意图与多轮增强

引入：

- 多轮会话状态
- 复合意图处理
- 澄清机制
- Grounding 优化

## 23.3 第三阶段：归因与解释增强

扩展：

- 异常检测
- 归因分析
- 自动摘要
- 图表推荐

## 23.4 第四阶段：平台化与治理

建设：

- 统一元数据平台
- 统一语义层
- 问数评测平台
- 反馈学习闭环
- 权限审计与成本治理

---

## 24. 最终结论

大模型实现智能问数，真正可上线的核心并不是“让模型直接写 SQL”，而是建立一套由以下能力共同组成的系统：

1. 稳定的业务语义层
2. 明确的意图识别体系
3. 结构化槽位抽取和中间表示 IR
4. 基于元数据和知识库的 Schema Grounding
5. 受约束的查询规划与生成
6. 严格的权限控制与结果校验
7. 可解释、可追问、可学习的闭环机制

一句话概括：

**智能问数不是“自然语言转 SQL”单点问题，而是“自然语言 -> 业务语义 -> 结构化分析意图 -> 可控查询执行 -> 可信结果解释”的完整系统工程。**
