# 大模型智能问数（NL2SQL / Conversational Analytics）架构设计与最佳实践

> 目标读者：架构师、算法工程师、数据平台负责人
> 核心命题：如何让业务人员用自然语言问一句话，就能拿到**准确、可信、可解释**的数据答案。

---

## 目录

1. [问题本质与核心挑战](#1-问题本质与核心挑战)
2. [整体架构设计（分层架构）](#2-整体架构设计分层架构)
3. [端到端详细流程](#3-端到端详细流程)
4. [核心难题一：语义理解](#4-核心难题一语义理解)
5. [核心难题二：意图识别](#5-核心难题二意图识别)
6. [最难问题及最佳解决方案](#6-最难问题及最佳解决方案)
7. [关键技术选型](#7-关键技术选型)
8. [工程化与性能优化](#8-工程化与性能优化)
9. [评估体系](#9-评估体系)
10. [落地路线图](#10-落地路线图)

---

## 1. 问题本质与核心挑战

### 1.1 智能问数是什么

智能问数 = **自然语言 → 结构化查询 → 数据结果 → 可解释回答**。

它不是简单的 NL2SQL 翻译，而是一条完整链路：

```
用户问题  →  语义理解  →  意图识别  →  Schema/指标映射  →  查询生成  →  执行  →  结果解读  →  可视化
```

### 1.2 为什么难 —— 三大鸿沟

智能问数最本质的困难，是三层"鸿沟"：

| 鸿沟 | 描述 | 例子 |
|---|---|---|
| **语义鸿沟** | 业务口语 ≠ 数据库字段 | "华东区销冠" → `region='East' AND rank=1`，但表里根本没有"销冠"字段 |
| **逻辑鸿沟** | 一句话 ≠ 一条 SQL | "同比增长最快的品类" → 需要自连接 + 窗口函数 + 排序 |
| **口径鸿沟** | 同一个词 ≠ 同一个定义 | "销售额" 到底含不含退款？含不含税？财务和运营口径不同 |

> **关键洞察**：90% 的失败不是 SQL 语法错误，而是"生成了一条语法完全正确、但业务含义错误的 SQL"。这就是所谓的**静默错误（Silent Failure）**，也是智能问数最危险的问题——用户拿到一个看似合理的数字，却做出了错误决策。

### 1.3 难度排序（从易到难）

```
单表聚合查询        ★☆☆☆☆   LLM 直接可解
多表 JOIN          ★★★☆☆   依赖 Schema Linking 质量
歧义消解            ★★★★☆   依赖上下文与澄清机制
指标口径一致性      ★★★★★   依赖语义层（最难，纯 LLM 无法解决）
正确性自证明        ★★★★★   依赖执行验证 + 多重校验
```

---

## 2. 整体架构设计（分层架构）

推荐采用 **"语义层为核心 + LLM 为推理引擎"** 的五层架构。这是当前业界（dbt Semantic Layer、Cube.js、阿里 QuickBI、AWS QuickSight Q）验证过的最佳范式。

```
┌─────────────────────────────────────────────────────────────┐
│  ① 交互层  Interaction Layer                                  │
│  多轮对话管理 · 澄清追问 · 结果可视化 · 反馈闭环               │
├─────────────────────────────────────────────────────────────┤
│  ② 理解层  Understanding Layer                                │
│  意图识别 · 实体抽取 · 时间归一化 · 上下文改写 · 歧义检测      │
├─────────────────────────────────────────────────────────────┤
│  ③ 语义层  Semantic Layer  ★核心★                            │
│  指标定义 · 维度体系 · 业务术语词典 · 口径统一 · 权限          │
│  （Metrics Store / Headless BI）                              │
├─────────────────────────────────────────────────────────────┤
│  ④ 生成层  Generation Layer                                   │
│  Schema Linking · RAG检索 · SQL/DSL生成 · 自我校验 · 纠错      │
├─────────────────────────────────────────────────────────────┤
│  ⑤ 执行层  Execution Layer                                    │
│  查询优化 · 沙箱执行 · 结果缓存 · 数据源适配（多引擎）        │
└─────────────────────────────────────────────────────────────┘
        ↑                                              ↓
   知识底座：向量库 · 元数据仓库 · Query日志 · 反馈样本库
```

### 2.1 为什么语义层是核心（最重要的架构决策）

**不要让 LLM 直接生成裸 SQL 去查物理表。** 这是最常见也最致命的架构错误。

正确做法：LLM 生成的是**面向语义层的查询意图**（Metric + Dimension + Filter），语义层再编译成物理 SQL。

```
❌ 错误架构：  用户 → LLM → 裸 SQL → 物理表
                    （口径不可控、幻觉字段、权限失控）

✅ 正确架构：  用户 → LLM → 语义查询(Metric/Dim/Filter) → 语义层编译 → 物理 SQL → 数据
                              （口径统一、字段可控、权限内置）
```

**语义层带来的价值：**

- **口径唯一**："销售额"在语义层只有一个定义 `SUM(amount) WHERE status='paid'`，LLM 无法"发明"新口径。
- **收敛搜索空间**：LLM 不需要在 500 张物理表里选，只需要在 50 个指标里选，准确率大幅提升。
- **权限与安全内置**：行列级权限在语义层统一控制，天然防止越权。
- **可维护**：物理表结构变了，只改语义层映射，上层无感。

---

## 3. 端到端详细流程

以用户提问 **"帮我看下上个月华东区销售额同比增长最快的前三个品类"** 为例，走完整链路：

### Step 0 — 预处理与安全过滤
- 敏感词/注入检测（防 Prompt Injection、SQL Injection）
- 输入规范化（去除冗余、繁简统一）

### Step 1 — 上下文改写（Query Rewriting）
多轮场景下，把指代/省略补全为独立完整问题：
```
用户上文："看下华东区销售额"
用户本轮："那前三名品类呢"
改写后："华东区销售额同比增长最快的前三个品类"
```
> 技术：用 LLM 做 **Coreference Resolution + Query Completion**，输入近 N 轮对话历史。

### Step 2 — 意图识别
分类当前问题属于哪种意图（详见第 5 节）：
```
意图 = 数据查询（Data Query）
子意图 = 排名类（Ranking）+ 趋势类（同比）
时间范围 = 上个月
```

### Step 3 — 实体抽取与归一化
```
维度实体：  华东区 → region        品类 → category
指标实体：  销售额 → metric:gmv
时间实体：  上个月 → [2026-06-01, 2026-06-30]  （相对时间归一化，关键！）
比较意图：  同比 → YoY，对比期 [2025-06-01, 2025-06-30]
限定：      前三 → LIMIT 3, ORDER BY yoy_growth DESC
```

### Step 4 — Schema Linking / 语义映射
把抽取的业务实体，映射到语义层的**指标和维度**（而非物理字段）：
```
"销售额"        → 语义层指标  metric.gmv
"华东区"        → 维度值      dim.region = 'East'
"品类"          → 分组维度    dim.category
"同比增长"      → 计算表达式  (cur - prev) / prev
```
> 这一步是准确率的命门。技术方案：**向量检索召回候选 + LLM 精排**（第 6 节详述）。

### Step 5 — 查询生成（生成语义 DSL，非裸 SQL）
LLM 生成结构化查询意图（推荐 JSON/DSL 中间表示）：
```json
{
  "metrics": ["gmv"],
  "dimensions": ["category"],
  "filters": [{"field": "region", "op": "=", "value": "East"}],
  "time_range": {"start": "2026-06-01", "end": "2026-06-30"},
  "comparison": {"type": "yoy"},
  "order_by": [{"field": "gmv_yoy_growth", "dir": "desc"}],
  "limit": 3
}
```

### Step 6 — 语义层编译 → 物理 SQL
语义层引擎（如 Cube/dbt-semantic-layer/自研）把 DSL 编译成方言 SQL：
```sql
WITH cur AS (
  SELECT category, SUM(amount) AS gmv
  FROM fact_orders
  WHERE region='East' AND status='paid'
    AND order_date BETWEEN '2026-06-01' AND '2026-06-30'
  GROUP BY category
),
prev AS (
  SELECT category, SUM(amount) AS gmv
  FROM fact_orders
  WHERE region='East' AND status='paid'
    AND order_date BETWEEN '2025-06-01' AND '2025-06-30'
  GROUP BY category
)
SELECT c.category,
       c.gmv,
       (c.gmv - p.gmv) / NULLIF(p.gmv,0) AS yoy_growth
FROM cur c LEFT JOIN prev p ON c.category = p.category
ORDER BY yoy_growth DESC
LIMIT 3;
```

### Step 7 — 自我校验（生成前/后双重校验）
- **语法校验**：SQL Parser 静态解析（EXPLAIN，不落数据）
- **语义校验**：LLM 反向解释这条 SQL 在算什么，与用户问题比对（Self-Consistency）
- **约束校验**：字段是否都在语义层白名单内、是否触发权限拦截

### Step 8 — 沙箱执行
- 只读连接、超时熔断、行数上限、成本预估（防止全表扫描拖垮库）
- 命中缓存则直接返回

### Step 9 — 结果解读与可视化推荐
- LLM 把结果表转成自然语言洞察："华东区上月同比增长最快的品类是A(+45%)、B(+38%)、C(+30%)"
- 图表类型自动推荐：排名类 → 柱状图；趋势类 → 折线图；占比类 → 饼图

### Step 10 — 反馈闭环
- 用户点赞/点踩/纠正 → 沉淀为 Few-shot 样本库 → 反哺 Step 4/5，持续进化。

---

## 4. 核心难题一：语义理解

语义理解要解决"**用户到底在说什么数据概念**"。

### 4.1 关键子任务

| 子任务 | 目标 | 主流技术 |
|---|---|---|
| 实体识别 NER | 抽出维度/指标/时间/条件 | LLM few-shot + 词典增强 |
| 时间归一化 | "上个月""近7天""Q3" → 具体日期 | 规则引擎 + LLM（相对时间必须交给确定性代码，不能让LLM算） |
| 业务术语解析 | "销冠""复购率""大盘" | **业务术语词典（同义词/黑话映射）** |
| 指代消解 | 多轮中的"它""那个" | LLM 上下文改写 |
| 隐含约束补全 | "销售额" 默认排除退款单 | **语义层默认过滤条件** |

### 4.2 语义理解的核心武器：业务术语词典（Glossary）

这是最容易被忽视、但性价比最高的手段。建立一张**业务黑话 → 语义层概念**的映射表：

```yaml
- term: 销冠 / 销售冠军
  maps_to: metric.gmv + rank=1
- term: 大盘 / 整体
  maps_to: 全域无维度过滤
- term: 复购率
  maps_to: metric.repurchase_rate  # 复用中台已定义口径
- term: 华东 / 华东区 / 江浙沪
  maps_to: dim.region IN ('East')
```

> **实践建议**：术语词典用向量化存储，检索时先做同义词召回，再让 LLM 判定。冷启动时从历史 Query 日志和 BI 报表标题里挖掘高频术语。

### 4.3 时间归一化 —— 必须交给确定性代码

**血泪教训**：绝对不要让 LLM 自己计算"上个月是几号到几号"。LLM 会算错（尤其涉及财年、周、季度边界）。

正确做法：LLM 只负责识别时间**类型**（`last_month`/`last_7_days`/`this_quarter`），由确定性的时间解析函数计算真实区间。

---

## 5. 核心难题二：意图识别

意图识别决定"**这个问题该走哪条处理链路**"。

### 5.1 意图分类体系（两级）

**一级意图（是否要走问数）：**

```
├── 数据查询类   → 走完整 NL2SQL 链路
├── 元数据类     "有哪些指标？""这个字段啥意思" → 查语义层/文档
├── 闲聊/无关     → 拒答或引导
└── 操作类       "导出""设置提醒" → 走 Function Call
```

**二级意图（数据查询的分析模式）—— 决定 SQL 模板：**

| 二级意图 | 触发词 | SQL 特征 |
|---|---|---|
| 明细查询 | 看下、列出、明细 | SELECT * / 无聚合 |
| 聚合统计 | 总数、平均、合计 | GROUP BY + 聚合函数 |
| 排名 Top-N | 最高、前N、排名 | ORDER BY + LIMIT |
| 趋势 | 趋势、走势、每天 | 时间维度 GROUP BY |
| 对比 | 同比、环比、对比 | 自连接 / 窗口函数 LAG |
| 占比 | 占比、构成、分布 | SUM() OVER() |
| 归因 | 为什么、原因 | 多维下钻（最复杂，常需多步） |

### 5.2 意图识别的技术方案对比

| 方案 | 准确率 | 延迟 | 可维护性 | 建议 |
|---|---|---|---|---|
| 规则/关键词 | 中 | 极低 | 差 | 仅做兜底 |
| 小模型分类器（BERT） | 高 | 低 | 中 | 高频固定意图 |
| **LLM few-shot 分类** | 高 | 中 | 好 | **首选** |
| **语义路由（向量+LLM）** | 很高 | 中 | 好 | **推荐组合** |

> **最佳实践**：**语义路由（Semantic Router）** —— 把典型意图样本向量化，用户问题先做向量相似度召回意图候选，再用 LLM 在候选里精判。兼顾准确率、成本和可扩展性。

---

## 6. 最难问题及最佳解决方案

以下是智能问数**最难、最关键**的四个问题，逐一给出最优解。

### 6.1 【最难】Schema Linking —— 语义映射准确率

**问题**：企业有几百上千张表、上万个字段，如何让 LLM 精准找到该用哪些表/字段？直接把全部 Schema 塞进 Prompt 既超长又低准。

**最佳方案：分层召回 + 精排（RAG for Schema）**

```
① 离线：为每个 表/字段/指标 生成"语义描述"并向量化入库
   （字段名 + 注释 + 枚举值样例 + 业务别名 + 使用示例）

② 在线召回：用户问题向量化 → 从向量库召回 Top-K 相关表/字段/指标
   （K 从上万收敛到 ~20）

③ LLM 精排：把召回的候选 Schema + 术语词典喂给 LLM，让它做最终选择

④ 生成：只基于选中的 Schema 子集生成查询
```

**关键增强手段：**
- **列值索引**：把高基数枚举值（如城市名、品类名）也建索引。用户说"上海"，能直接定位到 `city` 字段。
- **Schema 语义增强**：字段注释要写"人话"，`amt` → 注释"订单实付金额(元,含税不含运费)"。注释质量直接决定召回质量。
- **M-Schema 表示法**：用紧凑的、带样例值和外键关系的 Schema 表示，比裸 DDL 效果好。

### 6.2 【最危险】静默错误与正确性保障

**问题**：SQL 语法对、能跑出结果，但业务逻辑错——这是最致命的，因为用户会信任错误结果。

**最佳方案：多重防御 + 执行验证（纵深防御）**

```
防御层1  语义层约束     指标口径唯一，LLM 不能自定义 → 从源头消灭口径错误
防御层2  生成即校验     Schema 白名单校验，字段不存在直接拒绝，杜绝幻觉字段
防御层3  自然语言回译   LLM 把生成的 SQL 翻译回自然语言，与原问题比对
防御层4  执行前 EXPLAIN 静态分析成本与正确性，拦截全表扫描
防御层5  自洽性投票     生成 N 条 SQL，执行结果一致才采信（Self-Consistency）
防御层6  结果合理性     数值异常检测（如占比>100%、同比>1000%触发复核）
防御层7  透明可解释     把生成的 SQL + 口径说明展示给用户，让用户可复核
```

> **核心原则：宁可澄清，不可猜错。** 不确定时主动追问，比给一个错误答案强一万倍。

### 6.3 歧义消解 —— 主动澄清机制

**问题**："销售额" 是含税还是不含税？"用户数" 是新增还是累计？

**最佳方案：置信度驱动的澄清（Clarification）**

```
生成阶段计算映射置信度：
  ├── 置信度高（唯一匹配）      → 直接执行
  ├── 置信度中（多候选可辨）    → 反问选择："您指的销售额是①GMV ②实付金额?"
  └── 置信度低（无法匹配）      → 引导："我没找到'XX'指标,当前可用指标有..."
```

澄清要**给选项**而非开放式提问，降低用户负担。同时把澄清结果记入会话上下文，避免反复问。

### 6.4 复杂查询生成（同比/环比/嵌套/多步）

**问题**：归因分析、留存率、漏斗这类需要多步推理或复杂窗口函数的查询，一次性生成极易出错。

**最佳方案：CoT 分解 + 语义层预置 + Agent 多步**

- **能预置的绝不现算**：同比、环比、留存、漏斗、复购率这类固定分析模式，在**语义层预定义为计算指标**，LLM 只需引用，不用现写窗口函数。
- **Chain-of-Thought 分解**：复杂问题让 LLM 先拆解为子步骤，逐步生成、逐步验证。
- **Agentic 多轮**（归因场景）：先总量→再下钻各维度→定位异常维度，多次查询迭代，像分析师一样思考。

---

## 7. 关键技术选型

### 7.1 提示工程 vs 微调

| 维度 | 提示工程 + RAG | 微调（SFT/LoRA） |
|---|---|---|
| 冷启动 | 快 | 慢（需标注数据） |
| Schema 适配 | 灵活（RAG 动态注入） | 需重训 |
| 领域黑话 | 靠词典/few-shot | 效果更好 |
| 成本 | 推理成本高 | 训练成本高、推理省 |
| **建议** | **首选起步** | 有稳定高频场景 + 标注数据后再上 |

> **最佳组合**：**RAG（动态 Schema/术语/few-shot）为主 + 针对本领域 SQL 风格做轻量 LoRA 微调**。业界（如 DAIL-SQL、CodeS）验证：好的 Schema 表示 + 相似样例检索，比盲目微调更划算。

### 7.2 Few-shot 样例检索（提升准确率最有效手段之一）

维护一个"问题→正确SQL"的样例库，在线检索与当前问题最相似的 3-5 个样例注入 Prompt。样例库从：
- 人工标注的高质量种子
- 用户点赞/纠正的反馈样本
- BI 报表已有的 SQL 逆向生成

### 7.3 模型选型

- **理解/意图/改写**：中等模型即可（成本敏感，可用小模型）
- **SQL 生成/复杂推理**：用强推理模型（Claude/GPT-4 级或专用 Code 模型）
- **结果解读**：中等模型
- 分级路由，简单问题用小模型，复杂问题升级，平衡成本与质量。

---

## 8. 工程化与性能优化

### 8.1 缓存策略（三级）
```
L1 结果缓存    相同语义查询直接返回结果（注意数据时效性 + 权限）
L2 SQL 缓存    相同意图复用已验证 SQL，跳过生成
L3 Schema缓存  召回结果缓存，减少向量检索
```

### 8.2 延迟优化
- 意图识别、Schema 召回**并行**执行
- 流式输出：先返回"正在查询"和 SQL，结果异步补
- 简单查询走快速通道（模板匹配），绕过完整 LLM 链路

### 8.3 安全与治理
- **只读账号 + 行列级权限**：语义层统一鉴权，用户看不到的数据 LLM 也查不到
- **成本护栏**：查询前成本预估，超阈值需二次确认
- **Prompt 注入防护**：用户输入与系统指令隔离，输出 SQL 做白名单校验
- **审计日志**：每条问题→SQL→结果全链路可追溯

---

## 9. 评估体系

没有评估就没有优化。建立分层评估：

| 层级 | 指标 | 说明 |
|---|---|---|
| SQL 层 | **Execution Accuracy（执行准确率）** | 执行结果与标准答案一致率（比 Exact-Match 更实际） |
| SQL 层 | Valid SQL Rate | 语法/可执行率 |
| 映射层 | Schema Linking 准确率 | 表/字段/指标选对率 |
| 意图层 | 意图分类准确率 | |
| 体验层 | 澄清率 / 一次答对率 / 拒答合理率 | |
| 业务层 | 用户满意度 / 采纳率 | 最终北极星指标 |

**建立黄金评测集（Golden Set）**：覆盖各难度、各意图的"问题-标准SQL-标准结果"三元组，每次迭代回归测试，防止改进 A 场景却退化 B 场景。

> 参考公开基准：**Spider / Spider 2.0 / BIRD**（更贴近真实企业库，含脏数据和复杂 Schema），可用于横向评估模型能力。

---

## 10. 落地路线图

分阶段落地，先解决 80% 高频问题，再啃硬骨头：

```
阶段一（MVP，1-2月）
  · 单表/简单聚合查询
  · 基础语义层：核心 20-50 个指标
  · 术语词典冷启动
  · RAG Schema 召回
  目标：高频简单问题准确率 > 85%

阶段二（增强，2-3月）
  · 多表 JOIN + 排名/趋势/对比
  · 主动澄清机制
  · 自我校验 + 执行验证
  · Few-shot 反馈闭环
  目标：中等复杂度准确率 > 80%，静默错误率 < 3%

阶段三（进阶，持续）
  · 归因/漏斗/留存等复杂分析（Agentic）
  · 多轮深度对话
  · 领域 LoRA 微调
  · 全链路可观测 + 自动化评估
```

---

## 核心结论（一句话总结）

> **智能问数的胜负手不在于 LLM 多强，而在于三件事：**
> **① 建好语义层**（统一口径，收敛搜索空间，从架构上消灭口径错误）；
> **② 做好 Schema Linking**（RAG 召回 + 精排 + 术语词典，解决语义鸿沟）；
> **③ 保障正确性**（纵深防御 + 执行验证 + 主动澄清，消灭静默错误）。
>
> **LLM 是推理引擎，语义层是知识底座，工程校验是安全网。三者缺一不可。**

---

*文档版本 v1.0 · 架构范式：语义层为核心 + LLM 推理 + RAG 增强 + 纵深校验*
